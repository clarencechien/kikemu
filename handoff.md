# Handoff — kikemu 聽譯引擎評測

給接手的 Claude Code。這是**評測**,不是產品。目的是用數據回答一個問題,然後這個問題的答案會決定 kikemu 的架構。

---

## 1. 要回答的問題

**在導覽解說這個場景,「自訂詞表」值不值得成為產品核心?**

拆成三個可量測的子問題:

1. Gemini Live 一路到底(A),跟 Speechmatics 聽 + Gemini 譯(C),誰的專有名詞召回率高?
2. 掛上自訂詞典(C+)相對於不掛(C),提升多少?
3. 這個差距隨噪音增加是收斂還是放大?

**如果 C+ 相對 A 的優勢很小,就選 A** —— 一跳、少一個廠商、少一份維運。
**如果差距很大,詞表就是 kikemu 的資產**,整個產品要圍繞它設計。

---

## 2. 語料

### 主要來源(已驗證可用)

大阪觀光局「しっとんか大阪 / オーディオ タイムトラベル」,內容由 ON THE TRIP 製作。

索引頁:`https://osaka-info.jp/local_journey/audio-travel/`

**為什麼是它:**
- **mp3 直連可下載**,例:`https://osaka-info.jp/local_journey/wp/wp-content/uploads/2025/03/5a30d0933715447b6b86180fa7e818e7.mp3`
- **頁面上就有對應的日文旁白全文** —— 直接當標準答案,省掉人工轉寫
- 專業錄音的導覽解說,語域與目標場景完全一致
- 專有名詞密度極高(神名、社名、神事名、地名、年代)

三個系列,共 16+ 篇,每篇 2~3 段音檔:

| 系列 | 篇數 | slug |
|---|---|---|
| 東大阪三社めぐり | 4 | `higashiosaka_00` ~ `_03` |
| 池田まち歩き | 7 | `ikeda_01` ~ `_07` |
| 堺まち歩き | 5 | `sakai_01-02` ~ `sakai_10-12` |

索引頁底部有 SEE MORE,實際篇數更多。

### 三個必須先驗證的陷阱

1. **頁面文字不保證等於音檔內容。** 頁面自己寫「本記事では音声ガイドの一部をお楽しみいただけます」,全篇在 App 裡。文字可能是改寫版或節錄。
2. **音檔與段落不是 1:1。** 枚岡神社那篇的 B-1 與 B-2 指向**同一個 mp3**。抓下來要先聽過確認對應關係。
3. 因此:**先抽三段人工核對文字與音訊是否逐字吻合**,不吻合的就人工修正成真正的逐字稿再入庫。這一步不能跳過,標準答案錯了整份報告就沒意義。

### 備援來源

若上述吻合度太差,改用 **CSJ 日本語話し言葉コーパス**(`https://clrd.ninjal.ac.jp/csj/`)的講演獨白,線上版免費、逐字轉寫品質有保證。缺點是沒有導覽場景的音樂墊底與空間感。

### 授權

© OSAKA CONVENTION & TOURISM BUREAU / ON THE TRIP。**僅供本次私下評測使用**,不重新散布音檔、不公開評測用的音訊衍生檔,報告中只引用短句。抓取時加 User-Agent、限速,不要平行猛抓。

### 選取原則

選 **6 段**,每段 60~180 秒:
- 2 段來自東大阪(神道術語密集:アメノコヤネノミコト、しめかけ神事、かゆうら神事)
- 2 段來自堺(茶道與人物:千利休、侘び寂び、渡来文化)
- 2 段來自池田(近代人物與地名:小林一三、呉服座、サカエマチ商店街)

三個領域的詞彙不重疊,可以測「詞表要多專才有用」。

---

## 3. 噪音合成

原始音檔是錄音室品質,要人工降級成現場條件。**同一段內容跑五種聲學條件**,這樣差異純粹來自聲學。

| 代號 | 處理 | 模擬什麼 |
|---|---|---|
| **N0** | 原始 | 導覽機 line-in 的理想狀況 |
| **N1** | + RIR 卷積(中型室內,RT60 ≈ 0.6s) | 展館內手機收音 |
| **N2** | N1 + 人群噪音 @ SNR 15 dB | 一般參觀人潮 |
| **N3** | N1 + 人群噪音 @ SNR 8 dB | 尖峰時段 |
| **N4** | 帶通 300–3400 Hz + 輕度削波 + RIR | 擴音喇叭 |

**材料:**
- RIR:OpenSLR `RIRS_NOISES`
- 噪音:MUSAN 或 DEMAND(取 cafeteria / public square 類)

**實作要點:**
- 全部統一成 16 kHz 單聲道 PCM
- SNR 以語音段的 RMS 計算,不要含靜音段
- 固定亂數種子,確保可重現
- 卷積後做峰值正規化,避免削波污染結果
- 每個條件輸出獨立檔案,檔名帶條件代號

產出 6 段 × 5 條件 = **30 個音檔**。

---

## 4. 實驗矩陣

四個 arm × 五個聲學條件 = 20 格,每格 6 段。

| arm | 聽 | 譯 | 說明 |
|---|---|---|---|
| **A** | Gemini Live | 同一個模型 | 一路到底,用 manemu 的口譯 systemInstruction |
| **C** | Speechmatics 即時 | Gemini(manemu systemInstruction) | 兩跳,不掛詞典 |
| **C+** | Speechmatics + custom dictionary | 同上 | 詞典含 `sounds_like` 假名讀音 |
| **D** | Deepgram Nova-3 + keyterm | 同上 | 選配。用來比 100 詞 vs 1000 詞的差別 |

**C+ 與 D 的詞典怎麼來:**
不要手工列。**用該景點的公開資料自動生成**——官方頁面 + 維基百科丟給 Gemini 萃取專有名詞與讀音。這同時也在驗證產品裡「以地點為 key 自動備妥脈絡包」那條路可不可行。詞典生成的 prompt 與輸出要一併存檔。

Speechmatics 日文的 `sounds_like` 只吃全形平假名或片假名,單一詞條不超過 6 個字,超過會被靜默丟棄——生成後要做格式檢查。

**串流參數:**A 與 C 都走即時 WebSocket,不要用批次端點,否則量不到延遲也不反映真實條件。

---

## 5. 指標

### 主要指標

**專有名詞召回率。** 每段人工列出 15~30 個專有名詞(神名、人名、社名、神事名、地名、年代),數各 arm 抓對幾個。

判定規則寫死:完全字面相符才算對;假名與漢字不同寫法視為錯;部分正確不給分。規則寫進 README,不要臨場判斷。

**這是主要指標,因為全文差 2% 使用者無感,但「千利休」聽錯整句就毀了。**

### 次要指標

- **CER**(日文用字元錯誤率,不用 WER)—— 整體聽力健康度
- **譯文 adequacy** —— 沿用 manemu 那套五廠 LLM 評審面板,1~5 分
- **台灣用語命中** —— 掃譯文是否出現「視頻/質量/信息/軟件/網絡」等,計次
- **成本** —— 每段每 arm 的實際花費

### 延遲(只在即時 arm 量)

- 部分結果延遲:語音結束 → 第一版文字
- 定稿延遲:語音結束 → 文字不再變動
- **改寫率**:暫定文字被推翻的字元比例 —— 這是「讀起來累不累」的真正來源,多數評測不量,但對這個產品最關鍵

---

## 6. 產出

```
/corpus
  manifest.json        # 每段:來源 URL、標題、參考逐字稿、專有名詞清單
  raw/                 # 原始 mp3
  wav/                 # 16k 單聲道
  conditions/          # 30 個加噪檔
/scripts
  fetch.py             # 抓取 + 解析頁面文字(限速、加 UA)
  degrade.py           # 加噪,固定種子
  run_<arm>.py         # 各 arm 的 runner,輸出統一 JSON
  score.py             # 召回率、CER、用語掃描
  judge.py             # 接 manemu 評審面板
/results
  raw/                 # 每次呼叫的完整回應,含 usage
  report.md
```

每次呼叫的原始回應與 usage 都要留檔。**沒留檔的實驗等於沒做**,重跑一次是錢也是時間。

---

## 7. 報告要回答的

1. 四個 arm 在五個條件下的專有名詞召回率矩陣(主圖)
2. **C+ 減 C 的差值** —— 詞表的邊際價值,這是核心數字
3. **A 減 C+ 的差值隨噪音的走勢** —— 是否在乾淨條件下 A 就夠好、只有吵的時候才需要詞表
4. 譯文 adequacy 與台灣用語命中(預期 A 與 C 相同,因為共用 systemInstruction;若不同要查原因)
5. 每段成本與延遲
6. 一句話建議:kikemu 應該走哪個 arm,以及在什麼條件下要切換

---

## 8. 帳號與成本

- **Speechmatics**:註冊送 $100 額度、不用綁卡;即時每小時約 $0.24。本次實驗用量約 1 小時音訊 × 多次,**完全落在贈送額度內**
- **Gemini**:有免費層;Live 音訊輸入 $3/1M、文字輸出按文字費率
- **Deepgram**(選配):新帳號有 $200 額度
- 預估總支出:**接近零**,主要成本是時間

---

## 9. 開工順序

1. 抓 6 段音檔 + 頁面文字 → **人工核對吻合度**(這步不過就換 CSJ,不要硬做)
2. 人工列專有名詞清單
3. 寫加噪腳本,產 30 個檔
4. 跑 A 與 C(先不掛詞典),看基準線
5. 生成詞典,跑 C+
6. 視情況跑 D
7. 出報告

第 1 步是關卡。標準答案不可靠的話,後面所有數字都是假的。
