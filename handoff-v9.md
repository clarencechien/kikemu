# Handoff v9 — 把 Gemma 4 E4B / 12B 的坑補完

**目的:E4B 現在是「中文地端可用」的候選,但它的證據比 Breeze 薄很多。**
把它補到同一個水準,並清掉 v8 留下的三條 Gemma 侷限(16l、16n、16o)。

**判讀規則全部寫在 §3,先寫死,不看到數字才決定怎麼解釋。**

成本上限:**$1.00**(Modal dashboard 另設 $10 硬上限)。
費率 2026-08-19 查:L4 $0.000222/s、A100-40GB $0.000583/s。
實測吞吐(v8 量到的,用來估這份):
E4B on L4 **11.0s/chunk**、12B on A100 **20.0s/chunk**、E4B 短視窗 7.3s。

---

## 1. 要補的五格

| 格 | 內容 | GPU | 估計 |
|---|---|---|---|
| **G** | **E4B 聽 + Gemma 4 譯** —— 全地端鏈第二版 | 不用 | **$0.02** |
| **H** | E4B 噪音曲線(補 M1/M2) | L4 | $0.15 |
| **I** | E4B × exp1 全 6 段(補長段) | L4 | $0.16 |
| **J** | 貪婪對照:**12B** 與 E4B,量取樣變異 | A100 + L4 | $0.24 |
| **K** | E4B 的 bootstrap CI 與配對比較 | 不用 | **$0** |

**合計 ≈ $0.57**,上限 $1.00。

### G. E4B 聽 + Gemma 4 譯 —— 全地端鏈第二版(使用者指定先做)

| | |
|---|---|
| arm | `chain_e4b_gemma`、`chain_e4b_gemini` |
| 輸入 | **既有的** `exp5/results/raw/Xgma_e4b/` 六份轉寫,不重跑聽的那跳 |
| 譯 | `gemma-4-26b-a4b-it` 與 `gemini-3.5-flash`,`thinkingLevel: "minimal"`,凍結的 `INTERPRETER_SYSTEM` |
| GPU | **不用**,純 API |

**為什麼這格最該先做:** §3F.4 驗過的「全地端鏈」第一跳是 **Breeze**,
不是 Gemma。v8 之後 E4B 才有資格當第一跳,但**從沒接過**。
接起來只要 12 次 API 呼叫,就能回答一個實際問題:

> **要做中文的全地端聽譯,該用 Breeze 還是 Gemma 4 E4B 當耳朵?**

Breeze 的術語召回是 0.845、E4B 是 0.684,**但召回不等於下游譯文品質**——
Breeze 沒有標點、沒有分句,而 E4B 的輸出是帶標點的自然句。
那對第二跳是加分還是沒差,只有接起來才知道。**這是本任務書唯一可能改變
產品建議的一格。**

**五條鏈同料比較**(輸入音檔完全相同,譯的 prompt 都是凍結的
`INTERPRETER_SYSTEM`、都 `thinkingLevel: minimal`):

| 鏈 | 第一跳 | 第二跳 | 既有? |
|---|---|---|---|
| `chain_brz_gemma` | Breeze | gemma-4-26b-a4b | ✅ 0.717 |
| `chain_brz_gemini` | Breeze | gemini-3.5-flash | ✅ 0.795 |
| `chain_sm_gemini` | Speechmatics | gemini-3.5-flash | ✅ 0.626 |
| **`chain_e4b_gemma`** | **Gemma 4 E4B** | gemma-4-26b-a4b | ← 新(**純 Gemma 全地端**) |
| **`chain_e4b_gemini`** | **Gemma 4 E4B** | gemini-3.5-flash | ← 新(隔離第一跳) |

指標與 v8 F 完全相同:英文術語在譯文中的存活率、台灣用語誤用、簡體殘留。

### H. E4B 噪音曲線(侷限 16l 的一半)

| | |
|---|---|
| arm | 併入 `Xgma_e4b` |
| 語料 | exp5 3 段 × M1/M2 = **6 檔**,60 chunk |
| GPU | L4(與既有 M0/M3 同設定) |
| 估計 | 660s ≈ **$0.15** |

Breeze 有四條件曲線(§3E.2b),E4B 只有兩端。要把 E4B 當地端候選,
**掉幅是不是也平**必須知道——現在講的 0.114 只是兩點連線。

### I. E4B × exp1 全 6 段(侷限 16l 的另一半)

| | |
|---|---|
| arm | 擴充 `Xgma_ja_e4b` 從 2 段到 6 段 |
| 語料 | 四個長段 × N0–N4 = 20 檔,90 chunk |
| GPU | L4 |
| 估計 | 990s ≈ **$0.22** |

E4B 的日文只有 sakai 兩段、13 個專名,而 12B 已經補到 6 段 335 個專名(§3F.2h)。
**兩者的 n 不對等,現在的「E4B 0.400 vs 12B 0.323」是拿 13 個專名比 13 個專名**
——結論方向沒問題,但撐不起「日文不可用」這種否決性結論。

### J. 貪婪對照:**12B** 才是重點(侷限 16n / 16o)

| | |
|---|---|
| arm | `Zgma_greedy`(12B × 純中文視窗)、`Xgma_e4b_greedy_M0`(E4B × exp5 M0) |
| GPU | A100(12B)+ L4(E4B) |
| 估計 | 8×20s + 30×11s = 490s ≈ **$0.24** |

v8 只驗了 E4B 在純中文視窗上的取樣變異(0.312 → 0.295,小)。
**12B 完全沒驗**——而 12B 的整個故事是「退化生成、重複迴圈」。

> **如果 12B 的跳針在貪婪解碼下消失,那「12B 不能聽」這個結論就是錯的,
> 它只是被 `temperature: 1.0` 害的。** 這是目前風險最高的一條未驗證假設,
> 因為它已經寫進 README、stt-matrix、CLAUDE.md 鐵律 9。

E4B 那半是補在 **exp5 M0**(主表那一格),確認主表數字也穩。

### K. E4B 的 bootstrap CI(純計算,不花錢)

Breeze 與 SM 的比較都有 segment-cluster bootstrap CI(§3E.3),E4B 沒有。
用既有的 per-item outcome 檔補上 `E4B − SM batch`、`E4B − Breeze` 兩組,
5000 次、seed 20260819。**不呼叫任何 API。**

---

## 2. 執行順序與紀律

**順序:G → J → H → I → K。**

- **G 先**:使用者指定,而且它是唯一可能改變產品建議的一格。
- **J 第二**:它可能推翻已經寫進三份文件的結論,越早知道越好
  (這是 CLAUDE.md 鐵律 6「數字擴散」的教訓)。
- H / I 是把 E4B 的證據補厚,不會推翻什麼,排後面。
- K 最後,因為它要吃 H / I 的結果。

紀律照 handoff-v8 §3,不重述。額外兩條(v8 踩到的):

7. **`modal run` 一次最多送兩個**,四個會撞 `App create rate limit`。
8. **改動既有 arm 的腳本時,預設值不能變**——既有結果要能原樣重跑。

---

## 3. 判讀規則(**先寫死**)

### G(E4B 當耳朵的全地端鏈)

設 S = 英文術語在譯文中的存活率。基準是既有的
`chain_brz_gemma` **0.717**(目前最好的全地端鏈)。

| `chain_e4b_gemma` 的 S | 結論 |
|---|---|
| ≥ 0.717 | **E4B 當耳朵不輸 Breeze** → 中文全地端鏈可以用純 Gemma 組合,並記下「E4B 的標點/分句對第二跳有幫助」 |
| 0.65 ~ 0.717 | 略遜但同級 → 兩條都可用,**建議仍以 Breeze 為主**(它的 ASR 召回高 0.16) |
| < 0.65 | E4B 的召回劣勢**會傳到下游** → 全地端鏈的耳朵**指定 Breeze**,E4B 只當備案 |

**另外一個獨立判準(不與上表混談):**
比較 `chain_e4b_gemini` 與 `chain_brz_gemini`(第二跳都固定 Gemini,
只有耳朵不同)。差值 = **第一跳的純效果**。

| 差值 | 意義 |
|---|---|
| 與「ASR 召回差 0.845 − 0.684 = 0.161」同量級 | 召回差**原樣傳到下游**,ASR 召回是好的代理指標 |
| 明顯小於 0.161 | **第二跳把差距吃掉了一部分**——LLM 譯的時候會從上下文補回一些術語 |
| 方向相反(E4B 較好) | E4B 的標點與分句對第二跳的幫助**大於**它的召回劣勢,值得單獨寫一節 |

⚠️ 兩條鏈的轉寫**形式差很多**(Breeze 無標點、E4B 有),所以這不是乾淨的
單變因對照。結論一律寫「與……一致」,不寫「證明」。

### J(貪婪對照)—— **最重要的一格**

設 12B 在純中文視窗上的 CER:取樣 **2.231**、長度比 **2.21**。

| 12B 貪婪的結果 | 結論 |
|---|---|
| CER 仍 > 1.5 且長度比仍 > 1.8 | 退化生成是**模型性質**,現有結論全部成立,不用改任何文件 |
| CER 掉到 < 0.8 或長度比掉到 < 1.4 | **「12B 不能聽」是取樣造成的** → README、stt-matrix、CLAUDE.md 鐵律 9、報告 §3F.2b/2g **全部要改**,而且要把「沒驗貪婪就下結論」寫成新的踩雷筆記 |
| 介於兩者之間 | 標明「部分是取樣、部分是模型」,並把 12B 的所有數字改標成「取樣設定下」 |

E4B on exp5 M0:設既有 0.741。

| E4B 貪婪 M0 | 結論 |
|---|---|
| 差 < 0.05 | 主表數字穩,侷限 16n 可以從「未量測」改成「已量測、影響小」 |
| 差 ≥ 0.05 | **exp5 主表的 E4B 數字要標成單次取樣的點估計**,並補跑 M3 |

### H(E4B 噪音曲線)

沿用 §4 B 的規則(Breeze 那格):

- M1/M2 落在 M0–M3 連線上(偏離 < 0.03)→ 掉幅線性,0.114 的講法成立
- 中間塌陷 → 有門檻效應,說法要改成「在某個 SNR 之前掉幅小」
- 另外與 Breeze 的曲線並列:**掉幅比 Breeze 的 0.070 大多少**,
  決定 stt-matrix 要不要在「曲線最平」那句話後面加但書

### I(E4B × exp1 全 6 段)

設 R = E4B 在 6 段 335 個專名上的 tolerant 召回。門檻沿用 §4 C 的 **0.60**
(exp1 無詞表 SM 的水準),**不另設**。

| R | 結論 |
|---|---|
| ≥ 0.60 | 「日文不可用」要**撤回**——那是 13 個專名的小樣本結論 |
| 0.30 ~ 0.60 | 「日文不可用」成立,而且現在有 335 個專名撐著 |
| < 0.30 | 同上,且 E4B 的日文比 sakai 兩段看起來更差 |

**同時看切塊對照**(與 §4 E 對 12B 做的同一件事):
E4B 的 sakai(不切塊)vs 長段(切塊),除以 `Cplus` 當難度基準。

### K(bootstrap CI)

- `E4B − Xbat_bi` 的 95% CI **不含 0** → 「E4B 與 SM batch 同級」要改成有方向的敘述
- 含 0 → 「同級」成立,維持現有寫法
- `E4B − Xbrz_auto` 的 CI 不含 0 且為負 → Breeze 的優勢在統計上站得住

---

## 4. 驗收

- [ ] 每個新 arm 的 `_meta.json` 記下 GPU、模型、prompt 來源、**sampling 模式**
- [ ] 音檔 sha256 與 manifest 相符
- [ ] 判讀依 §3 的表,不臨場改標準
- [ ] 偏離任務書之處寫進本檔末尾
- [ ] 報告、`stt-matrix.md`、README、CLAUDE.md 同步更新;數字對得上 `results/`
- [ ] **J 若推翻結論,四份文件一起改,不留舊數字**

## 5. 明確不做

- ❌ **新語料。** 唸稿型中文那個變因(侷限 16g)仍不在範圍內。
- ❌ **E2B 補跑。** v8 已確認它在純中文上 8/8 空輸出,不值得再投入。
- ❌ **12B 的 exp5 貪婪全跑。** J 只在純中文視窗上驗 12B(8 個檔、$0.09),
  因為那組的失效模式最明確。若 J 推翻結論,再另開任務書全跑。
