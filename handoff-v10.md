# Handoff v10 — ElevenLabs Scribe 評測(即時 + 非即時 × 三種語言)

**為什麼做這格:** `stt-matrix.md` 的即時推薦是 **Speechmatics 串流 + 詞表**,
而 **Scribe v2 Realtime 是它的直接競爭者**(~150ms、WebSocket、90+ 語言含日文中文),
本專案**從沒跟它比過**。這是目前唯一可能改變 kikemu **主線選型**的未執行實驗。

**判讀規則全部寫在 §4,先寫死,不看到數字才決定怎麼解釋。**

牌價 **2026-08-20 查**(他們 2026-08 才調降過,抄價務必附日期,鐵律 6):
批次 $0.22/hr、即時 $0.39/hr(產品頁另標 $0.28,**兩頁不一致,跑之前先確認**)、
keyterms +$0.05/hr、entity detection +$0.07/hr。
**不需要訂閱**:免費方案儲值(PAYG)即可打 API。

---

## 1. 先講一個會決定設計的規格差異

| | Speechmatics(現行) | Scribe |
|---|---|---|
| 詞表容量 | **1000 詞** | 批次 `keyterms`(上限待確認)、**即時上限 50 詞 × 20 字元** |
| 讀音標註 | ✅ `sounds_like`(全形假名) | ❌ **沒有對應機制** |
| 詞級時間戳 | ✅ | ✅(批次回傳 `start` / `end`;即時待驗) |
| 介面 | WebSocket | REST(批次)+ WebSocket(即時) |

**kikemu 的 exp1 詞表是每個地點 142~166 條,而且每一條都帶假名讀音。**
Scribe 即時只吃得下 **50 條、且沒有讀音欄位**。

> 這代表「Scribe 即時 vs SM 即時+詞表」**天生不是對等比較**。
> 若直接比,量到的會是「詞表容量 + 讀音標註」的差,不是引擎的差。
> **所以 §2 一定要有詞表對齊的控制組(S5),否則這個實驗做完沒有解釋力。**

---

## 2. 五格(語言 × 即時/非即時 全覆蓋)

| 格 | 語料 | 語言 | 即時? | 對照組 | 音訊 | 估計 |
|---|---|---|---|---|---|---|
| **S1** | exp1 6 段 × N0–N4 | **日文** | 批次 | `Cbplus` SM 批次+詞表 | 43.8 min | $0.20 |
| **S2** | exp5 3 段 × M0/M3 | **中英夾雜** | 批次 | `Xbat_bi` 0.699 / Breeze 0.845 | 30 min | $0.14 |
| **S3** | exp5 純中文 20 秒視窗 × 8 | **純中文** | 批次 | Breeze CER 0.198 / SM 0.206 | 2.7 min | $0.02 |
| **S4** | exp1 6 段 × N0–N4 | **日文** | **即時** | **`Cplus` SM 即時+詞表 0.791** ← 主線 | 43.8 min | $0.33 |
| **S5** | exp1 6 段 × N0–N4 | 日文 | 即時 | **SM 即時,詞表砍到同樣 50 詞** | 43.8 min | $0.18(SM) |

**合計 ≈ $0.87。** 三種語言、即時與非即時**都覆蓋到**。

### 語言覆蓋對照(回答「都有涵蓋嗎」)

| 語言 | 批次 | 即時 |
|---|---|---|
| **日文**(產品本業) | S1 | **S4 + S5** |
| **中英夾雜** | S2 | ⬜ 不做,理由見下 |
| **純中文** | S3 | ⬜ 不做,理由見下 |

**中英夾雜與純中文不做即時的理由:**
kikemu 的即時主線是**日文導覽**;中文那兩條的產品情境(逐字稿、法遵)本來就是
非即時的(`stt-matrix.md` 情境矩陣)。即時那格每格要 **1× 實時推流 44 分鐘**,
花的是 wall-clock 不是錢。**先把主線那格做掉,中文即時等有需求再說。**

### 選配:exp2(中英夾雜演講,645 詞 domain vocab)

**不列入主線。** exp5 已經涵蓋中英夾雜而且是**領域外**(exp2 對 Breeze 有訓練污染,
對 Scribe 雖然無污染,但兩份語料的結論會重複)。
exp2 唯一多出來的東西是「詞表 vs 語言 pack 的槓桿」對照,而
**Scribe 沒有「語言 pack」這個概念**,那個對照在它身上沒有對應物。
真要跑,批次 52.6 分鐘 ≈ $0.24。

---

## 3. 執行紀律

沿用 handoff-v8 §3,並補三條 Scribe 專屬的:

1. **先打一個檔驗形狀**(`--probe`),確認 `language_code`、`keyterms`、
   詞級 `start`/`end` 都如文件所述,再全跑。
2. **即時那格要先驗併發上限**。exp5 踩過:SM 的 realtime 併發上限是 1,
   殘留 session 會把整條路堵死(報告侷限 18)。Scribe 的上限**未知**,
   全跑之前先開兩條連線試。
3. **音檔一律由本機上傳並先對 manifest sha256**,與其他 arm 是同一份位元。

---

## 4. 判讀規則(**先寫死**)

### S4 + S5:主線那格(最重要)

三個數字並列(全部 exp1、tolerant 專名召回、N0–N4 micro):

```
A = Scribe 即時 + 50 keyterms
B = SM 即時 + 50 詞(詞表對齊控制組)
C = SM 即時 + 完整詞表 142~166 詞 = Cplus 0.791(現行產線)
```

**先看 A − B(引擎的差,詞表已對齊):**

| A − B | 結論 |
|---|---|
| ≥ +0.05 | **Scribe 的引擎在日文上比 SM 強** → 值得評估換主線,下一步是查它能不能吃更多詞 |
| −0.05 ~ +0.05 | 引擎打平 → **選型由詞表容量與讀音標註決定,而那兩項 SM 完勝** |
| ≤ −0.05 | Scribe 引擎較弱 → 主線維持 SM,本格結案 |

**再看 A − C(現實的差,含詞表劣勢):**

| A − C | 結論 |
|---|---|
| ≥ 0 | 即使詞表被砍到 50 條仍不輸現行產線 → **強訊號**,認真評估遷移 |
| −0.10 ~ 0 | 輸一截但在同一個級別 → 記錄,不動主線 |
| < −0.10 | **不可用於本業**,與 Breeze 日文那次同樣處理(§2.1c) |

⚠️ **A − C 不可單獨引用**:它混著引擎與詞表兩個變因。
要講「Scribe 比較差」只能引 A − B。

### S1(批次 × 日文)

對照 `Cbplus`(SM 批次 + 詞表)。門檻同上表(±0.05 / −0.10),**不另設**。
另外單獨看 **N3(人群 8dB)**:那是全套語料唯一能觸發「整段放棄」的條件。

| N3 表現 | 結論 |
|---|---|
| 零空輸出且召回 > 0.3 | 不會整段放棄,寫進 §6 優點 |
| 出現空輸出 | 與 Gemini Live(2 個)、Gemma 4(1 個)並列記錄 |

### S2(批次 × 中英夾雜)

對照 `Xbat_bi` 0.699(SM batch)與 `Xbrz_auto` 0.845(Breeze)。

| Scribe | 結論 |
|---|---|
| ≥ 0.845 | **超過 Breeze** → 非即時中文線的推薦要重排,Breeze 不再是天花板 |
| 0.699 ~ 0.845 | 勝 SM 不勝 Breeze → 記進矩陣,地端推薦不變 |
| < 0.699 | 中英夾雜上不如 SM batch,結案 |

### S3(批次 × 純中文,CER)

| Scribe CER | 結論 |
|---|---|
| ≤ 0.198 | **全場最低**,Breeze 的「純中文最強」要改寫 |
| 0.198 ~ 0.263 | 落在 Breeze 與 Gemini 批次之間,照實記 |
| > 0.263 | 純中文上不如 Gemini 批次 |

### 共通:一定要另外記錄的三件事

1. **詞級時間戳實際拿不拿得到**(文件說有,要看回傳)——
   拿不到的話 kikemu 的兩層時間軸接不上,那比召回率更決定性。
2. **即時的 partial / final 延遲**,與 `Cplus` 的 partial p50 0.37s / final p50 2.27s 對比。
3. **keyterms 上限實測**(即時文件說 50 × 20 字元;批次未寫)——
   直接送 100 條看會不會被拒。

---

## 4b. 計分前的正規化決定(**先寫死,在看到分數之前**)

probe 時發現兩件 Scribe 獨有的輸出特性,計分方式必須先決定,否則就變成
「看到分數再挑對自己有利的算法」:

1. **Scribe 會輸出 audio event 標記**(實測 exp5 出現 `[笑声]`)。
   **計分前一律移除 `[...]` 方括號標記**——那是註記不是轉寫,
   而且**沒有任何其他 arm 會產生它**,不移除等於平白讓它的 CER 變差、
   輸出長度變長。移除規則寫進計分腳本,並記在 arm 的 meta。
2. **Scribe 的中文輸出是簡體**(`凭证`、`网路`、`对`)。
   這與 Speechmatics 的 `cmn` 一樣(exp5 量到 SM 有 669 個簡體字),
   而既有計分本來就做 OpenCC S2T 折疊,**不需要為它改規則**。
   但這件事要寫進報告:**Scribe 中文線下游一定要接 OpenCC**,
   而且要照 `docs/related-work.md` §5.1 逐欄位判斷,不要全域套。

## 5. 驗收

- [ ] 每個 arm 的 `_meta.json` 記下 model_id、`language_code`、keyterms 條數、
      是否啟用 diarize、以及**牌價與查價日期**
- [ ] 音檔 sha256 與 manifest 相符
- [ ] 判讀依 §4 的表,不臨場改標準
- [ ] **S5 一定要跑**——沒有詞表對齊控制組,S4 的數字沒有解釋力
- [ ] 偏離任務書之處寫進本檔末尾
- [ ] 報告、`stt-matrix.md`、README 同步更新;數字對得上 `results/`

## 5b. 驗收(S1/S2/S3/S5,2026-08-20 完成;S4 未做)

- [x] 每個 arm 的 `_meta.json` 記下 model_id、`language_code`、keyterms 條數、
      diarize、**牌價與查價日期**
- [x] 音檔 sha256 逐檔算並寫進 meta
- [x] 判讀依 §4 的表,不臨場改標準
- [x] **S5 跑了**(批次那半 `Cbplus50`)——沒有它 S1 沒有解釋力
- [x] 報告(§2.1d)、`stt-matrix.md`(§6b)、README、`related-work.md` 同步更新
- [ ] **S4 即時那格未做**——主線的答案還沒有

### 結果對照 §4 的預先寫死規則

| 規則 | 門檻 | 實測 | 落點 |
|---|---|---|---|
| **A − B**(引擎,詞表對齊) | ≥ +0.05 → Scribe 引擎較強,下一步查能不能吃更多詞 | **+0.096** | **第一列**:引擎較強;已查:**批次吃得下 166 條** |
| **A − C**(含詞表劣勢) | −0.05 ~ +0.05 → 同級 | **+0.009** | 帶著 50 詞劣勢仍與現行產線打平 |
| **S1 N3** | 零空輸出且召回 > 0.3 | 零空輸出,**0.642 / 0.657** | **不會整段放棄**,而且是全場 N3 最高 |
| **S2** | ≥ 0.845 → 超過 Breeze,非即時中文線推薦要重排 | **0.921** | **第一列**:Breeze 不再是天花板 |
| **S3** | > 0.263 → 純中文不如 Gemini 批次 | **0.333** | 第三列成立(但見下面的 metric 偏差) |

**實際花費:$0.45**(S1 $0.197 + 完整詞表那格 $0.197 + S2 $0.11 + S3 $0.012 + probe;
估的是 S1+S2+S3 = $0.36)。SM 的控制組另計在 SM 帳號。

## 6. 明確不做

- ❌ **新語料。** 全部用既有的 exp1 / exp5 / zh_windows。
- ❌ **中文的即時格**(見 §2 理由)。
- ❌ **entity detection 加購**($0.07/hr)——kikemu 用不到,不花這個錢。
- ❌ **拿 A − C 單獨下「Scribe 比較差」的結論**(混變因,見 §4)。

---

## 7. 偏離紀錄

1. **多跑了兩個不在任務書裡的 arm,兩個都是規則指名的下一步或必要控制組。**
   - `Sbat_ja_full`(Scribe + 完整 166 詞):§4 的規則寫「A − B ≥ +0.05 →
     下一步是查它能不能吃更多詞」。查了(50/100/166 都回 200),所以補跑。
   - `Cbplus50`(SM 批次 + 50 詞、無讀音):任務書的 S5 原本只設計了**即時**的
     控制組,但 S1 是批次,沒有批次控制組就沒有解釋力。**批次那半先做了**,
     即時那半等 S4。

2. **S4(即時)沒做。** 需要 WebSocket 推流 harness,而且 43.8 分鐘音訊要
   1× 實時推。批次的結果已經寫進文件,但**每一處都標明「批次贏不等於即時贏」**
   ——Gemini 那次正好是反例(批次 0.567 vs Live 0.030)。

3. **`Cbplus50` 取詞表的前 50 條,不是挑過的 50 條。** 挑選過會更強,
   所以 B 這個基準偏保守,**A − B = +0.096 可能高估引擎的差**。已寫進報告 §2.1d。

4. **發現我們自己 metric 的一個偏差,而且是看到 Scribe 分數之後才發現的。**
   純中文視窗的參考**沒有標點**,而 CER 把每個標點算成一個 token,
   系統性懲罰會輸出標點的引擎(Scribe 0.333 → 去標點 0.205、
   Gemini 0.263 → 0.159)。**沒有事後換算法**:主數字維持原樣,
   去標點那組明確標成事後敏感度分析,並開了侷限 16r。
   **受影響的是舊結論**:「Breeze 純中文 CER 全場最低」只在含標點的算法下成立。

5. **exp5 那格沒有給 Scribe 任何詞表**(exp5 本來就沒有 SM 詞表 arm 可對照),
   所以 0.921 是「無詞表」下的數字。有詞表能到多少未測(侷限 22)。
