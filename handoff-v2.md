# Handoff — 晶晶體(中英夾雜)聽譯評測

接在 kikemu 評測之後的第二個實驗,**共用同一套 harness**。目錄結構、統計處理、報告格式全部沿用,只換語料、詞表來源與指標。

---

## 1. 要回答的問題

kikemu 的結論是「詞表值得成為核心資產」,但那是在**純日文**導覽語料上驗證的。這次要問:

1. **同一個詞表機制,在中英夾雜的技術演講上是否同樣有效?**
2. **詞表需要「這一場的投影片」,還是「一份領域通用詞表」就夠?** —— 這決定產品要不要每次做會前準備
3. **哪些英文不該翻?** 翻譯層把該保留的英文譯成中文,是這個領域獨有的失敗模式

第 2 題是產品問題,不是技術問題,但只有這個實驗答得出來。

### 為什麼晶晶體之前沒被測到

kikemu 報告的 §2.8.3 掃了 90 份譯文,結論是「晶晶體不是問題,只有 3 處裸露英文」。**那測的是輸出端**——譯文裡有沒有冒出英文。但語料是純日文旁白,**輸入端從來沒有語碼混合**。

真正的晶晶體是輸入端問題:「這個 case 要 follow up 一下 schedule」。那是 ASR 任務,完全未測。

---

## 2. 語料

### 來源

台大李宏毅教授的課程錄影(機器學習 / 生成式人工智慧導論)。

- 影片:YouTube 公開
- 投影片:課程網頁 `speech.ee.ntu.edu.tw/~hylee/` 公開 PDF

**為什麼是它:**
- 中文授課、英文術語密度極高且不可替代(Transformer、Self-attention、BERT、GAN、Autoencoder、Meta Learning)
- **投影片就是現成的術語清單**,標註成本趨近於零
- 主題可切分,能測「跨主題的詞表泛化性」
- 錄影音軌品質穩定,可作為降級的乾淨起點

### 選取

**3 段 × 5 分鐘,主題不重疊:**

| 段 | 主題 | 預期術語型態 |
|---|---|---|
| S1 | Self-attention / Transformer | 架構名詞,多為複合詞 |
| S2 | GAN / Diffusion | 模型名 + 訓練術語 |
| S3 | 生成式 AI 導論 | 較口語,術語與中文混得更碎 |

選取原則:避開播放投影片影片、避開學生提問段(收音差且非講者)、取連續 5 分鐘不剪接。

### 授權(重要)

課程公開 ≠ 開放授權。

- **僅供本次私下評測**,不重新散布音檔或衍生音訊
- 報告中只引用短句佐證,不貼長段逐字稿
- 抓取時限速、加 User-Agent
- 若要公開報告,先確認課程頁的授權聲明;**台大開放式課程(NTU OCW)通常採 CC 授權,若需要可公開的版本,改用 OCW 上的課程(例:李琳山教授數位語音處理概論)**

---

## 3. 標準答案

### 3.1 中文逐字稿

沿用 kikemu 的**雙引擎驗證協定**(不是單人聽寫):

- 兩個獨立引擎轉寫(Speechmatics batch、Gemini 3.5 Flash)
- 三方對齊,殘差逐條人工檢視,結論寫進 `corpus/reference/review.md`
- 兩引擎一致而人工未察的插入 → 採納
- 正字法爭議一律以人工判定為準,不偏向任一受測引擎
- 規則寫死在腳本,先於任何 arm 執行

**這次沒有官方文字可對照(不像 osaka-info 有頁面文字),所以人工檢視的比重更高。預估每段 30~45 分鐘。**

### 3.2 英文術語清單(主要指標的正解)

**這是本實驗最省力的部分,也是關鍵設計。**

1. 從逐字稿中抽出**實際講出來的**英文術語(含縮寫、複合詞)
2. 標註每個術語的出現次數與時間位置
3. **凍結**於任何 arm 執行前

**判定規則(兩套並列,均寫死):**

- **strict**:大小寫不敏感,但拼寫需完全相符
- **tolerant**:接受凍結的標準變體(`self-attention` / `self attention` / `Self Attention`;`GAN` / `gan`;`BERT` / `Bert`)

變體清單在執行前凍結,不得事後追加。

---

## 4. 實驗 arm

**詞表來源是本次的核心變因**,共三種:

| arm | 聽 | 詞表來源 | 回答什麼 |
|---|---|---|---|
| **X** | SM 即時(zh 或 multi) | 無 | 基準線 |
| **X-domain** | 同上 | **通用 ML/AI 術語表**(從公開 glossary 取,**不含本課程投影片**) | 領域包夠不夠? |
| **X-slides** | 同上 | **本堂課的投影片 PDF** 自動抽取 | 會前準備值多少? |
| **G** | Gemini Live 一路 | — | 一體式對照 |

譯文層全部走 Gemini 3.5 Flash,共用同一份 systemInstruction(見 §6)。

### 洩漏控制

**X-slides 的詞表來自投影片,而標準答案來自逐字稿——兩者來源不同,但內容高度重疊。這不是洩漏,這就是產品場景**(真實會議你本來就拿得到投影片)。

**X-domain 才是控制組**:它證明「不做會前準備、只用領域包」能走多遠。兩者的差距就是「會前準備」的價值。

### 語言模式

Speechmatics 要測兩種設定,這本身就是一個發現:

- `language: cmn`(單語中文)
- `language: multi`(多語自動偵測,支援句內語碼轉換)

**預期單語模式會把英文術語中文化,多語模式較好——但要數據佐證,不要假設。**

---

## 5. 聲學條件

**與 kikemu 不同,不要沿用食堂人聲。** 目標情境是會議室與講堂,降級方式也不同:

| 代號 | 處理 | 模擬 |
|---|---|---|
| **M0** | 原始錄影音軌 | 會議系統輸出 / line-in |
| **M1** | + 講堂 RIR(T60 ≈ 0.8s) | 大教室後排 |
| **M2** | M1 + 空調底噪 @ SNR 20dB | 一般會議室 |
| **M3** | M1 + 多人交談底噪 @ SNR 12dB | 開放辦公室 / 討論中的會議 |

- 不做擴音失真(N4):此情境不適用
- RIR 從 MIT IR Survey 以 T60 決定性挑選,規則寫死
- 底噪用 DEMAND 的 `OMEETING` / `OOFFICE`(辦公室實錄),**不要用食堂**
- SNR 以語音活動段 RMS 計算;亂數種子固定;每步後峰值正規化

3 段 × 4 條件 = **12 檔**。

---

## 6. 指標

### 6.1 主要:英文術語召回率

各 arm × 各條件的 micro-average,附 segment-cluster bootstrap 95% CI 與 McNemar 配對計數。

**核心數字是 X-slides − X-domain**:會前準備的邊際價值。

### 6.2 失敗模式分類(新增,kikemu 沒有)

把每個未命中的術語歸類,這比單一數字更有產品價值:

| 代號 | 描述 | 例 |
|---|---|---|
| **F1 諧音化** | 英文被聽成中文諧音 | Transformer → 傳司佛馬 |
| **F2 誤認他字** | 聽成別的英文字 | attention → intention |
| **F3 語言崩潰** | 整段語言判定錯誤,英文完全消失或中文被當英文 | — |
| **F4 遺漏** | 該處無輸出 | — |

各 arm 的失敗模式分佈。**若 X 的失敗集中在 F1,詞表應該有效;若集中在 F3,問題在語言模式設定,詞表救不了。**

### 6.3 過度翻譯率(譯文層,本實驗獨有)

台灣工程師說「這個 attention 的部分」,譯文若成為「這個注意力的部分」反而更難懂。**有些英文不該翻。**

- 定義:術語清單中的詞,在譯文裡**被譯成中文**的比例
- 分兩組回報:
  - **不該翻**(模型架構名、專有名詞:Transformer、BERT、GAN)
  - **可翻可不翻**(一般技術詞:attention、embedding、loss)
- 測兩種 systemInstruction:
  - **P0**:kikemu 原版(未特別指示)
  - **P1**:加入「詞表中的英文術語維持原文,不譯」

**P1 − P0 的差距就是這條規則的價值,而它會直接搬到廠區線**(料號、機台型號、製程術語同樣不該翻)。

### 6.4 次要

- 中文部分 CER
- 延遲:partial p50、final p50/p90、改寫率
- 成本:每段每 arm 實際花費(留 usage 記錄)

---

## 7. 產出

沿用 kikemu 的結構:

```
/corpus
  manifest.json          # 來源 URL、主題、參考逐字稿、術語清單與變體
  raw/ wav/ conditions/
  reference/review.md    # 殘差人工檢視紀錄
  vocab/
    domain.json          # 通用 ML 術語表(控制組)
    slides_S1..S3.json   # 各段投影片抽出的詞表
/scripts
  build_reference.py     # 規則寫死
  make_terms.py          # 術語抽取 + 變體凍結
  degrade.py             # 固定種子
  run_<arm>.py
  score.py               # 召回率 + 失敗模式分類 + 過度翻譯率
  judge.py
/results
  raw/                   # 每次呼叫完整回應含 usage
  report.md
```

**每次呼叫的原始回應與 usage 必須落檔。沒留檔的實驗等於沒做。**

---

## 8. 報告要回答的

1. 四個 arm × 四個條件的英文術語召回率矩陣
2. **X-slides − X-domain**:會前準備值不值得(核心數字)
3. **X-domain − X**:通用領域包單獨的價值
4. 單語模式 vs 多語模式的差距
5. 失敗模式分佈 —— 問題出在聽、還是出在語言判定
6. **P1 − P0**:「不譯規則」的價值
7. 一體式(G)在這個領域是否同樣隨噪音崩潰
8. 一句話建議:廠區/會議線應該用哪個組合

---

## 9. 開工順序

1. 選 3 段、下載、切 5 分鐘
2. **雙引擎轉寫 + 人工核對逐字稿** ← 關卡,預估 2 小時
3. 抽術語清單、凍結變體
4. 產通用詞表(不碰投影片)與投影片詞表
5. 加噪,產 12 檔
6. 跑 X / X-domain / X-slides,單語與多語各一輪
7. 跑 G
8. P0 / P1 兩版 systemInstruction 各跑譯文層
9. 出報告

第 2 步是關卡。這次沒有官方頁面文字可對照,**標準答案不可靠的話後面全是假的**。

---

## 10. 預估成本

- Speechmatics:0.6 小時音訊 × 多輪,遠低於 $100 贈送額度
- Gemini:翻譯層與詞表生成,< $1
- 評審面板:< $0.5
- **總計 < $3**

主要成本是第 2 步的人工核對時間。
