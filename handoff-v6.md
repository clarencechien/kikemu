# Handoff v6 — exp5:領域外對照(X-breeze 的污染控制)

**這份任務書存在的唯一理由**:Breeze ASR 25 的訓練資料含 `NTUML2021`(11 小時),
那正是 exp2 的 S1 / S2 來源課程。**模型在我們的評測語料上微調過**,
所以 Phase A 的漂亮數字(§3D)不能拿來說「Breeze 比 Speechmatics 好」。

exp5 要回答的就一句話:**換一批模型沒看過的台灣華語中英夾雜語料,優勢還在不在?**

---

## 1. 為什麼不是先跑 Phase B

Phase B 是同一批語料跑滿 12 檔。對 S1/S2 而言,那只是把污染過的數字算得更精確。
**先做 exp5,再視結果決定 Phase B 值不值得跑。**

| | 回答什麼 | 目前價值 |
|---|---|---|
| Phase B(12 檔) | S1/S2/S3 的完整矩陣 + CI | 低:S1/S2 污染,只有 S3 有效 |
| **exp5(本任務書)** | **優勢是否可推廣** | **高:主結論成立與否取決於它** |

---

## 2. 語料:已實測選定(不是猜的)

`exp5/scripts/probe_density.py` 對候選節目抓 5 分鐘樣本、跑 Speechmatics `cmn_en`、
算「每分鐘拉丁字母段落數」。實測結果在 `exp5/corpus/candidates.json`。

**核心發現:晶晶體密度取決於受訪者是否在談自己動手做的實作,不取決於節目或主題。**

| 集 | 主題 / 受訪者 | 密度(段/分) | 採用 |
|---|---|---|---|
| E344 | 賽事轉播技術 / 技術長 | **4.8** | ✅ T1 |
| E324 | 數位憑證皮夾 / 數發部工程師 | **3.0** | ✅ T2 |
| E362 | 資安創業 / CTO(談創業) | 2.4 | 備援 |
| E412 | AI agent 工程 / 工程師 | 5.2 | ❌ 語域偏 AI |
| E419 | 自駕車圖資 / 產業副總 | 1.2 | ❌ 太稀疏 |
| E414 | 無人機 / 大學教授 | 1.2 | ❌ 太稀疏 |

對照標尺(exp2):S1 10.4、S2 4.0、S3 4.8。

**還要再選 1 段**(目標 T3),條件:第三位講者、工程實作主題、非 AI/ML、密度 ≥3.0。
用同一支探針量,不要用聽的或用主題猜。

### 為什麼不用 COSCUP(雖然授權最乾淨)

COSCUP 明文 CC BY-SA 4.0、講者眾多、語域正確,本來是首選。但:
議程 JSON 的 `record` 欄位**全部是空的**,議程頁也沒有影片連結,
**沒有可機讀的「議程 → 影片」對照**;影片在 YouTube,而資料中心 IP
(本機與 Colab 皆然)會被 bot 驗證擋下。

podcast RSS 的實務優勢很大:**MP3 直連可下載**,整條取得流程能自動化,
不需要人工把檔案放進雲端硬碟。列 COSCUP 為備援(若日後需要「會議室實錄」的聲學條件)。

### 明確排除

**ASCEND 與 CSZS-zh-en 不可用**——那是 Breeze 模型卡自己報的 code-switching
benchmark,拿它們測等於用開發者最佳化過的靶。

---

## 3. 設計:比 Phase B 小,但足以回答問題

3 段 × 2 條件 × 4 arm = 24 次推論。

**條件只取兩端**(exp2 已證實中間條件不會翻轉排序):
- `M0` 原始(peak-normalized)
- `M3` 混響 + 多人交談 12dB —— X-breeze 在 exp2 的最大贏面就在這裡

**arm**(全部沿用既有腳本,不新增引擎):
| arm | 說明 |
|---|---|
| `X_bi` | Speechmatics `cmn_en`,無詞表 ← 主要對照 |
| `Xsli_bi` | SM + 詞表(這裡沒有投影片,改用**節目 show notes** 抽詞) |
| `G` | Gemini Live 一體式 |
| `Xbrz_auto` | Breeze ASR 25 |

聲學條件**必須沿用 `exp2/scripts/degrade.py`**(同一顆 RIR、同一份 DEMAND 噪音、
同一組種子),否則與 exp2 的數字沒得比。

---

## 4. 正解怎麼建(這是最貴的一步,已經想過怎麼省)

exp2 當初是三路轉寫 + 80 處分歧逐條人工裁決。這裡可以砍到約三分之一:

1. 三路轉寫:SM `cmn_en`、Gemini、Breeze
2. **只對拉丁字母段落做人工裁決**(中文部分取多數決)——
   核心指標是英文術語召回,中文的細微差異不影響它
3. 凍結術語清單與變體(同 `exp2/scripts/make_terms.py` 的規則)
4. **裁決必須在任何 arm 的正式評分之前完成**,且裁決者不知道哪個轉寫來自哪個引擎

⚠️ **不可以拿 Breeze 的輸出當正解來源之一再去評 Breeze。** 三路轉寫裡它是一路,
所以裁決時必須盲化;若人力不足,寧可只用 SM + Gemini 兩路。

---

## 5. 判讀規則(先寫死,免得看到數字才決定怎麼解釋)

設 Δ = X-breeze 減 X_bi 的 tolerant 召回。

| exp5 的 Δ | 結論 |
|---|---|
| **≥ +0.15**,且 M3 上仍領先 | 優勢可推廣。Breeze 是真正的候選引擎,值得跑 Phase B 與地端評估 |
| **+0.05 ~ +0.15** | 有優勢但被污染放大。結論寫「領域內強、領域外小贏」,不改架構 |
| **≈ 0 或為負** | Phase A 的領先**主要來自訓練污染**。X-breeze 結案,報告據此收斂 |

**M3 要單獨看**:Phase A 最強的訊號是抗噪(0.780 vs SM 各 arm 0.42~0.54)。
記住逐字內容不會讓模型在噪音下更會聽,所以就算 M0 的優勢消失,
**M3 的優勢若還在,那才是這個模型真正有價值的地方**。

---

## 6. 驗收

- [x] 三段語料各自量過密度並落檔(`exp5/corpus/candidates.json`)
- [x] 音檔指紋進 manifest;聲學條件由 `exp2/scripts/degrade.py::build()` 產生
- [x] 正解建立過程盲化,拉丁段逐條裁決紀錄可查
      (`exp5/corpus/reference/overrides.json`,16 條;**無人耳終審**,
      `_protocol` 已記錄這條與 exp2 的差異)
- [x] 四個 arm 的輸出 schema 與既有 arm 一致
- [x] 判讀依 §5 的表,不臨場改標準 → Δ=+0.146,落在「+0.05~+0.15」那格
- [x] 報告更新:新增 §3E;§3D 的「能說 / 不能說」已依 exp5 逐條重寫
      (⟳ 標出三條改變的結論),節末並加了指向 §3E 的後續說明

**執行時偏離任務書的三處(都寫進報告 §3E.7 與侷限 16–21):**

1. SM 跑 **batch** 而非 realtime——帳號 realtime 併發上限 1,殘留 session 會堵死整條路。
   方向已知:唯一同檔對照上 batch 比 realtime **低 0.057**,即 batch 低估 SM,
   因此 Δ=+0.146 是偏高的估計(換 realtime 基準約 +0.09)。
2. Breeze 跑 **CPU/fp32** 而非 notebook 的 T4/fp16——本環境無 GPU。
   其餘參數相同(`exp5/scripts/run_breeze_cpu.py`),數值差異未量測。
3. 加跑兩個任務書沒列的 arm:`Gbat`(gemini-3.5-flash `generateContent`)與
   `Gbat37`(3.7-flash)。前者是為了回答「不需即時時 Gemini 多快多省」,
   後者是因為參考轉寫由 3.5-flash 產生,需要一個**沒參與造參考**的獨立對照。

## 7. 音檔授權

podcast 音訊**不進 repo**(同 exp1/exp2 慣例),只存 RSS URL、集數、切片位移與 sha256。
`exp5/scripts/probe_density.py` 可從 RSS 重新取得並重建,流程完全自動。
