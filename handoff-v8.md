# Handoff v8 — Modal 上的 Breeze 與 Gemma 補測

**目的:把地端這條線的證據補齊。** 目前推薦 Breeze 當地端引擎,但它的證據
全部來自中文語料;而「全地端聽譯鏈」還掛著「未驗證」。

**判讀規則全部寫在 §4,先寫死,不看到數字才決定怎麼解釋。**

成本上限:**梯次一 $0.35、梯次二 $1.50**,合計不到 $2(Modal 額度 $30,
dashboard 另設 $10 硬上限)。費率 2026-08-19 查:
T4 $0.000164/s、L4 $0.000222/s、A100-40GB $0.000583/s。

---

## 1. 梯次一(先做)

### A. Breeze × exp1 —— 日文,五個聲學條件

| | |
|---|---|
| arm | `Xbrz_ja` |
| 語料 | exp1 全部 6 段 × N0–N4 = **30 檔,43.8 分鐘** |
| GPU | T4 / fp16(與 exp2 Phase A 同設定,**不是** exp5 那次的 CPU/fp32) |
| 設定 | `chunk_length_s=0`、revision `cffe7ccb`、`language` 不指定 |
| 估計 | 996s ≈ **$0.16** |

**為什麼排第一:**

1. **Breeze 從沒跑過日文。** 現有證據是 exp2(訓練污染)與 exp5(中文領域外)。
   kikemu 的產品本業是日文導覽——推薦一個引擎給產品用,卻沒在產品的語言上測過。
2. **exp1 的 N3(人群 8dB)是目前唯一能觸發「整段放棄」的條件。**
   Gemini Live 在那格 0.030、Gemma 4 12B 0.077 且有一個檔輸出**完全是空的**。
   Breeze 會不會也崩,是 `stt-matrix.md` §6 現在標「未測」的那一條。
3. exp1 的參考來自大阪觀光局頁面,**無主場偏差**——這是最乾淨的比較場地。

**要改的既有檔**:`scripts/score.py` 的 `ARMS_JA` 加 `"Xbrz_ja": "transcript"`,
並讓它跟 `Abat` / `Xgma_*` 一樣跳過翻譯層指標。

### F. Breeze 聽 + Gemma 4 譯 —— 全地端鏈端到端

| | |
|---|---|
| arm | `Xbrz_auto_translate_gemma` |
| 輸入 | **既有的** `exp5/results/raw/Xbrz_auto/` 六份轉寫,不重跑聽的那跳 |
| 譯 | `gemma-4-26b-a4b-it`,`thinkingLevel: "minimal"`,凍結的 `INTERPRETER_SYSTEM` |
| GPU | **不用**,純 API |
| 估計 | **$0.05** |

**為什麼幾乎免費卻該做:** 報告 §3F.3 說「全地端鏈 = Breeze 聽 + Gemma 4 譯」,
但標著**未驗證**。兩端各自的數字都有了,中間沒接起來過。接起來只要一次
API 呼叫,就能把那句話從未驗證變成已驗證(或推翻)。

**對照組**:同一批 Breeze 轉寫改用 `gemini-3.5-flash` 譯,
以及現行產線 SM 轉寫 + Gemini 譯。**三條鏈同料比較。**

**指標**(exp5 是中英夾雜,所以看英文在譯文裡活下來沒有):

- 英文術語在譯文中的保留率
- 簡體殘留字數
- 台灣用語誤用(`scripts/score.py` 的 `TW_BAD` 清單)

### B. Breeze × exp5 M1/M2 —— 補完噪音曲線

| | |
|---|---|
| arm | 併入既有的 `Xbrz_auto` |
| 語料 | exp5 3 段 × M1/M2 = **6 檔,30 分鐘** |
| GPU | T4 / fp16 |
| 估計 | 711s ≈ **$0.12** |

exp5 只跑了 M0/M3 兩端。補中間兩格就有完整的噪音響應曲線,
才能說「掉幅」是不是線性的——現在講的 0.070 掉幅只是兩點連線。

> ⚠️ **這一格會與既有的 M0/M3 不同 dtype**(既有是 CPU/fp32,新跑是 T4/fp16)。
> 要嘛連 M0/M3 一起重跑(多 $0.12),要嘛在報告裡標明混用。
> **建議連 M0/M3 一起重跑**——$0.12 買一致性,而且順便驗證
> CPU/fp32 與 T4/fp16 的差異(那是 exp5 侷限 17 標「未量測」的一條)。
> 改成 **12 檔、60 分鐘、$0.22**。

**梯次一合計:$0.16 + $0.05 + $0.22 = $0.43**

---

## 2. 梯次二(梯次一有結果後再決定)

### C. Gemma 4 **E4B** × 三組語料

| | |
|---|---|
| arm | `Xgma_e4b`(exp5)、`Xgma_ja_e4b`(exp1)、`Zgma_e4b`(純中文視窗) |
| GPU | L4(E4B 小很多,不需要 A100) |
| 估計 | 1221s ≈ **$0.27** |

**假說**:12B 的 CER 2.231 有大量幻覺灌水(輸出長度 2.21 倍)。
**小模型的幻覺傾向常常較低**,E4B 可能反而穩。若成立,
「Gemma 4 不能聽」要改成「Gemma 4 **12B** 不能聽」。

### D. Breeze 三卡速度基準

| | |
|---|---|
| 內容 | 同一個 5 分鐘檔在 T4 / L4 / A100 各跑一次 |
| GPU | 三種 |
| 估計 | ≈ **$0.09** |

地端部署要報成本,目前只有 T4(2.9× 實時)與 CPU 4 核(0.20×)兩個點,
中間全靠猜。三個點才畫得出曲線。

### E. Gemma 4 12B × exp1 全部 6 段

| | |
|---|---|
| arm | 擴充 `Xgma_ja` 從 2 段到 6 段 |
| GPU | A100(長段要 30 秒切塊) |
| 估計 | 1842s ≈ **$1.07** |

目前日文只有 sakai 兩段(13 個專名)。補滿 6 段 = 67 個專名,
n 才夠支撐「日文 0.323」這個數字。

**梯次二合計:$1.43**

---

## 3. 執行紀律(前面白燒 $1.26 換來的)

每個 arm 一律照這個順序,不可跳步:

1. **讀模型卡的「限制」章節**,把上限抄進腳本註解
   (Gemma 4:30 秒音訊上限、audio 放 text 之後、不要改官方 prompt)
2. **先跑官方樣本當基準線**——分不出「模型不行」與「你不會用」就不要往下跑
3. **`--probe N` 抽驗**,看實際輸出再全跑
4. **每個檔/chunk 印一行進度**,不盲等
5. 音檔**由本機上傳並先對 manifest sha256**,不在容器裡重新產生
6. 結果落檔後立刻 commit,不累積

---

## 4. 判讀規則(**先寫死**)

### A(Breeze × 日文)

設 Δ = Breeze 減 **SM+詞表**(`Cplus`)的 tolerant 召回,取 N0–N4 平均。

| Δ | 結論 |
|---|---|
| ≥ 0 | Breeze 在日文上與最佳雲端方案平手或更好 → 地端推薦不必加語言限定 |
| −0.10 ~ 0 | 可用但輸一截 → 地端推薦要標「日文略遜」 |
| < −0.10 | **地端推薦必須限定語言**:「中文可、日文不可」 |

**N3 單獨看**(這是排第一的理由):

| N3 表現 | 結論 |
|---|---|
| 沒有空輸出、召回 > 0.3 | Breeze **不會**整段放棄 → 它比 Gemini Live / Gemma 4 更穩,寫進 §6 優點 |
| 出現空輸出或召回 < 0.1 | **「整段放棄」不是特定模型的毛病,是這個聲學條件本身超出所有模型** → §2.1b 的說法要再收窄一次 |

### F(全地端鏈)

| 結果 | 結論 |
|---|---|
| 三個指標都不比「SM + Gemini」差超過 10% | 全地端鏈**成立**,§3F.3 的「未驗證」改成已驗證 |
| 任一指標差超過 10% | 標明差在哪一跳(Breeze 的轉寫還是 Gemma 的譯) |
| Gemma 譯的輸出出現幻覺/跳針 | 全地端鏈**不成立**,§3F.2 的「第二跳可用」要收窄成「在乾淨轉寫上可用」 |

### C(Gemma 4 E4B)

**假說**:12B 的 CER 2.231 有大量幻覺灌水(輸出長度 2.21 倍),小模型幻覺傾向較低。
**這個假說要用兩個獨立的量來判,不可混談。**

先看**輸出長度比**(hyp 長度 ÷ 參考長度),它決定假說本身成不成立:

| E4B 的長度比 | 對假說的意義 |
|---|---|
| 明顯 < 12B 的 2.21(比如 ≤ 1.4) | 幻覺確實隨模型變小而降 → 假說的前半成立,再看下表的準確度 |
| 仍 ≈ 2.2 或更高 | 幻覺不是模型大小的函數 → **假說推翻**,不必再談「小模型比較穩」 |

再看**準確度**(exp5 用術語召回、exp1 用專名召回、純中文視窗用 CER):

| E4B vs 12B | 結論 |
|---|---|
| 三組都跨過可用門檻(exp5 ≥ 0.30、exp1 ≥ 0.60、純中文 CER ≤ 0.50) | 「Gemma 4 不能聽」要收窄成「Gemma 4 **12B** 不能聽」,並補測 E2B |
| 有進步但沒跨過門檻 | 記成「小模型較不幻覺但仍不可用」,選型結論不變 |
| 與 12B 同級或更差 | 可以**拿掉 12B 這個限定**:Gemma 4 這一代的音訊 ASR 在自發口語的中文/日文上都不可用 |

門檻的來源不是拍腦袋:0.30 是 exp5 最差的可用 arm(SM batch M3 0.646)的一半、
0.60 是 exp1 無詞表 SM(0.639)的水準、CER 0.50 是「還能看懂大意」的常用界線。
**寫在這裡就不再改。**

### C-bis(Gemma 4 **E2B**)—— 使用者指定補測

**規則落點說明:** §4 C 寫的是「三組門檻都跨過才補測 E2B」,實測兩過一沒過,
所以**照規則本來不跑**。這一格是使用者明確指定要補的——
**規則管的是自動觸發,不管使用者指定**,兩者不衝突,但要記下來這不是規則跑出來的。

E4B 的結果讓「小模型幻覺較低」的假說前半成立。E2B 比 E4B 再小一半,
**所以它問的是一個新問題:這個趨勢是單調的,還是有甜蜜點?**
判讀規則先寫死:

| E2B vs E4B(三組合看) | 結論 |
|---|---|
| E2B ≥ E4B(任一組更好、其餘不明顯更差) | 趨勢**單調**:在這個家族裡越小越穩,值得往 E2B 部署(它更省) |
| E2B ≈ E4B(三組都在 ±0.05 / CER ±0.05 內) | **兩者可互換**,選 E2B(一樣準但更省) |
| E2B < E4B(明顯更差) | **不是單調的,E4B 是甜蜜點** → 報告侷限 16k 從「不能說小模型一定比較好」 升級成**已有反證**,而且「參數量不是代理指標」這條更強:兩個方向都不成立 |

**另外看輸出長度比**(這是 E4B 那格真正的診斷量):

| E2B 的長度比 | 意義 |
|---|---|
| ≈ 1.0(像 E4B 的 1.04) | 退化生成隨模型變小而消失,與 12B 的 2.22 對照成立 |
| 明顯 > 1.4 | 退化生成**不是**模型大小的單調函數 → §3F.2g 的機制解釋要收窄 |

門檻與 §4 C 同一組(exp5 ≥ 0.30、exp1 ≥ 0.60、純中文 CER ≤ 0.50),**不另設**。

### D(Breeze 三卡速度)

這格沒有假說,是純測量——但**怎麼用**這三個數字要先講好,否則會變成「A100 最快」這種
沒有決策價值的結論。一律換算成**每小時音訊的 GPU 成本** = 卡的費率 ÷ 實時倍率:

| 結果 | 寫進 `stt-matrix.md` 的建議 |
|---|---|
| T4 的 $/hr 音訊最低 | 「地端 Breeze 用 T4 就夠,大卡買的是延遲不是成本」 |
| L4 最低 | 改推 L4,並標明 T4 便宜但慢到不划算 |
| A100 最低 | 反直覺,要附上原始秒數讓人自己驗 |

**還有一個比速度更重要的旁證**:同一個檔在三張卡上的轉寫**應該逐字相同**
(同 revision、同 dtype、同 `chunk_length_s`)。若不同 → 記成
**「跨 GPU 不可重現」**,那會影響所有 Breeze 數字的可信度,優先級高於速度。

### E(Gemma 4 12B × exp1 全部 6 段)

目前日文的 0.323 只有 sakai 兩段、13 個專名。補滿 6 段 = 67 個專名。
設 R = 6 段 × N0–N4 的 tolerant 專名召回(micro)。

| R | 結論 |
|---|---|
| \|R − 0.323\| ≤ 0.10 | 原數字站得住,只是 CI 變窄;報告數字換成 R,結論不變 |
| R < 0.22 | sakai 兩段是**簡單樣本**(短、可整檔送),0.323 是高估 → 報告要改寫 |
| R > 0.42 | 0.323 被小樣本低估 → 報告要改寫,並重看 §3F.2f 的變因拆解 |

**這格最有價值的其實是副產品**:6 段裡 sakai 兩段 < 30 秒(不切塊)、
其餘四段 97~158 秒(要切 4~6 塊)。**同一個模型、同一批語料、同一個 prompt,
只有切塊與否不同**——這是全專案唯一能單獨量出「切塊 handicap」的設計。

| 切塊四段 vs 不切塊兩段 | 結論 |
|---|---|
| 切塊段明顯較低(差 ≥ 0.10) | 切塊 handicap 是真的,§3F.2d 的第一條 handicap 從定性變定量 |
| 兩者相當 | 切塊不是 exp5 低分的原因,§3F.2f 的拆解再多一條佐證 |

⚠️ 這個對照**不是隨機分派**:短段與長段的內容、語速、專名密度本來就不同,
所以它是**觀察性**證據,不是因果。結論一律寫成「與……一致」而不是「證明」。

### B(噪音曲線)

- 若 M1/M2 落在 M0–M3 的連線上 → 掉幅是線性的,現有的 0.070 講法成立
- 若中間塌陷 → 有門檻效應,「掉幅小」的說法要改成「在某個 SNR 之前掉幅小」
- **CPU/fp32 vs T4/fp16**:M0/M3 重跑後與既有結果比對。
  差異 < 0.02 → exp5 侷限 17 可解除;≥ 0.02 → 標明數值受 dtype 影響

---

## 5. 驗收(梯次一 A/F/B,2026-08-19 結案)

- [x] 每個 arm 的 `_meta.json` 記下 GPU、dtype、模型 revision、prompt 來源
      —— `results/raw/Xbrz_ja/_meta.json`、`exp5/results/raw/Xbrz_gpu/_meta.json`、
      `exp5/results/raw/chain_*/_meta.json`
- [x] 音檔 sha256 全部與 manifest 相符(A/B 逐檔算並寫進 meta;F 不碰音檔)
- [x] 判讀依 §4 的表,不臨場改標準(三格的判讀對照見下面「結果」)
- [x] 偏離任務書之處寫進本檔末尾
- [x] 報告與 `stt-matrix.md` 同步更新;數字對得上 `results/`
      —— 曲線可用 `python3 exp5/scripts/noise_curve.py` 重現

### 結果對照 §4 的預先寫死規則

| 格 | 規則門檻 | 實測 | 落點 |
|---|---|---|---|
| **A** | Δ < −0.10 → 地端推薦必須限定語言 | Δ(Breeze − `Cplus`)= **−0.376** | 限定語言:**中文可、日文不可** |
| **A**(N3 單獨) | 沒有空輸出且召回 > 0.3 → 不會整段放棄 | 零空輸出、零極短,召回 **0.179** | 規則兩端都不完全命中:**不放棄**成立,但召回未過 0.3 → 記成「一直在講話,只是講錯」 |
| **F** | 三指標都不比 SM+Gemini 差超過 10% → 鏈成立 | 0.717 vs 0.626 = **+0.091**;台灣用語 0、簡體 0 | **成立**,且優於雲端兩跳 |
| **B** | M1/M2 落在 M0–M3 連線上 → 掉幅線性 | 偏離 **−0.002 / +0.015** | 線性,無門檻效應;曲線全場最平 |
| **B**(dtype) | 差 < 0.02 → 侷限 17 可解除 | **+0.000 / +0.000** | **解除** |

**實際花費(估算,不是帳單):**

| 來源 | 數字 |
|---|---|
| A 的 GPU 推論秒數(逐檔 meta 加總) | 625s + 載入 8.7s |
| B 的 GPU 推論秒數(逐檔 meta 加總) | 941s + 載入 4.5s |
| A+B 五次 `modal run` 的 app 存活時間合計(`modal app list`) | **2051s** |
| × T4 $0.000164/s(2026-08-19 查) | **≈ $0.34 上界** |
| F(純 API,12 次 `generateContent`) | < $0.01 |

app 存活時間**含 image build 與模型下載**,那些不佔 GPU,所以 $0.34 是**上界**,
真值介於 $0.26(純推論)與 $0.34 之間。**估的是 $0.38(A+B),吻合。**

> ⚠️ **這不是帳單數字。** Modal CLI 沒有查費用的指令,權威數字在 dashboard 的
> Cost Summary。要引用花費請以那裡為準;此表只是「量級對不對」的自查。

---

## 5b. 驗收(梯次二 C/D/E,2026-08-19 結案)

- [x] 每個 arm 的 `_meta.json` 記下 GPU、dtype、模型、prompt 來源
      —— `exp5/results/raw/Xgma_e4b/`、`exp5/results/raw_zh/Zgma_e4b/`、
      `results/raw/Xgma_ja_e4b/`、`results/raw/Xgma_ja/`(長段部分)
- [x] 音檔 sha256 逐檔算並寫進 meta
- [x] **先跑官方樣本當基準線**:E4B 在 `journal1.wav` 上轉寫完美,
      與 12B 是同一條基準線(鐵律 7)
- [x] 判讀依 §4 的表,不臨場改標準
- [x] 偏離任務書之處寫進本檔末尾
- [x] 報告與 `stt-matrix.md` 同步更新;數字對得上 `results/`

### 結果對照 §4 的預先寫死規則

| 格 | 規則門檻 | 實測 | 落點 |
|---|---|---|---|
| **C**(長度比) | ≤ 1.4 → 幻覺隨模型變小而降,假說前半成立 | exp5 **1.04**、純中文 **1.18**(12B 是 2.22 / 2.21) | **假說成立** |
| **C**(準確度) | 三組**都**跨門檻 → 收窄成「12B 不能聽」 | exp5 0.684 ≥ 0.30 ✅、CER 0.312 ≤ 0.50 ✅、exp1 日文 0.400 < 0.60 ❌ | **兩過一沒過,規則未涵蓋**——不改門檻,記成語言條件式結論 |
| **C**(E2B) | 三組都跨過才補測 E2B | 沒有都跨過 | **不補測** |
| **D**(成本) | 哪張卡的 $/hr 音訊最低 | T4 **$0.155** < L4 $0.175 < A100 $0.412 | 「T4 就夠,大卡買的是延遲不是成本」 |
| **D**(可重現) | 三卡轉寫應逐字相同,否則優先處理 | **逐字相同**(2199 字) | 通過,無「跨 GPU 不可重現」問題 |
| **E**(R) | \|R − 0.323\| ≤ 0.10 → 原數字站得住 | R = **0.272**,差 **0.051** | 第一列:**站得住**,n 從 13 → 67 專名 |
| **E**(切塊) | 差 ≥ 0.10 → 切塊 handicap 是真的 | 相對 `Cplus` 基準 0.333 → 0.346 | 第二列:**兩者相當**,切塊不是低分的原因 |

**梯次二實際花費(估算,不是帳單):**

| 項 | GPU | 推論秒數(逐檔 meta 加總) | 費率 | 估算 |
|---|---|---|---|---|
| C(exp5 60 + 純中文 8 + exp1 10 = 78 chunk) | L4 | 795s | $0.000222/s | **$0.18** |
| D(三卡各兩次 + 載入) | T4 / L4 / A100 | — | 混合 | **$0.13** |
| E(exp1 四長段 × 5 條件 = 90 chunk) | A100-40GB | 750s | $0.000583/s | **$0.44** |

**純推論合計 ≈ $0.75。** 上界用 app 存活時間算(含 image build 與模型下載,
那些不佔 GPU):梯次二七次 `modal run` 合計 **2584s**,即使全按 A100 費率
也只有 $1.51,實際多數跑在 L4/T4。

**估的是 $1.43,實際落在 $0.75 ~ $1.0 之間——低於估計。**
主因是 E 的每 chunk 只花約 8 秒(日文輸出短),而估計是照 exp5 的
20s/chunk 推的;E4B 也比 12B 快得多。

> ⚠️ **帳單權威在 dashboard 的 Cost Summary,Modal CLI 查不到費用。**
> 上表只是「量級對不對」的自查。

---

## 6. 明確不做

- ❌ **新語料**。本任務書只用既有的 exp1 / exp2 / exp5。
  「唸稿型中文」那個變因(報告侷限 16g)需要新語料,不在範圍內。
- ❌ **Gemma 4 12B 的 prompt 調校**。實測在官方 prompt 後面加一句就會壞,
  這一格已經確認無解。
- ❌ **exp2 的 Phase B**。S1/S2 訓練污染,跑完只是污染條件下的上界。

---

## 7. 偏離任務書之處(梯次一結案時補,v6/v7 慣例)

1. **B 的 arm 名沒有「併入既有的 `Xbrz_auto`」,改成新開 `Xbrz_gpu`。**
   §1 B 寫的是併入。但併入等於用 T4/fp16 的結果覆蓋 CPU/fp32 的,
   那樣就**永遠比不出 dtype 差異**——而那正是 §4 B 第二條規則要驗的東西。
   兩份都留才驗得了。既有的 `Xbrz_auto`(CPU/fp32)一個字沒動。

2. **`Xbrz_ja` 的評分跳過翻譯層指標。** `scripts/score.py` 的 `ARMS_JA` 加了
   `Xbrz_ja`,但它和 `Abat` / `Xgma_*` 一樣只有轉寫沒有譯文,
   TW 用語 / 簡體 / adequacy 那幾欄不適用,直接跳過而不是填 0
   ——填 0 會讓它在表上看起來「零失誤」。

3. **新增 `exp5/scripts/noise_curve.py`。** 任務書沒要求,但 §5 驗收要「數字對得上
   `results/`」,而 `compare.py` 只印 M0/M3(其他 arm 只有那兩格)。
   曲線的四個數字若只存在報告的表裡就沒有出處,違反 CLAUDE.md 鐵律 6。

4. **§4 A 的 N3 判準兩端都不完全命中。** 規則寫「沒有空輸出**且**召回 > 0.3
   → 不會整段放棄」,實測是零空輸出但召回 0.179。**沒有臨場改門檻**:
   照規則這格不算「通過」,所以結論寫成描述而不是背書——
   「它一直在講話,只是講錯」,並附上「幻覺比空白更難發現」這個反面。

5. **兩個自己寫出來的 bug(都是 B 這格)。**
   - `run_breeze_modal_exp5.py` 的 entrypoint 原本也叫 `main`,與
     `run_breeze_modal.py` 撞名(它 import 同一個 `modal.App`,同一個 app
     不可有兩個同名 entrypoint)。改名 `exp5_all`。
   - manifest 查詢寫成 `man[s]["sha256"]`,實際結構是
     `man["conditions"][s]["sha256"]`,第一次 B 跑到寫檔才炸。
     **教訓與 §3 執行紀律第 5 條同源**:對 manifest 這件事本身沒錯,
     錯在沒先用一個檔驗證寫檔路徑就送 12 個。

6. **commit 31795d0 的 message 帶的是 10/12 的部分結果**(當時 T3 的 M2/M3
   還沒回來):M2 0.863、掉幅 +0.063、dtype M3 +0.007。
   12/12 全數完成後的正確數字是 **M2 0.848、掉幅 +0.070、dtype 兩格都 +0.000**,
   以本檔與 `results/` 為準。commit message 不可事後修改,故在此更正。

---

## 8. 梯次二的偏離紀錄

1. **§4 原本只寫了 A/F/B 的判讀規則,C/D/E 沒有。** 在跑之前補寫進 §4
   (commit 在所有梯次二的執行之前),不是事後補。

2. **Gemma runner 的 GPU 改由環境變數 `KIKEMU_GEMMA_GPU` 決定。**
   Modal 的 `gpu=` 是裝飾期常數,不能當 CLI 參數;§2 C 要 L4、E 要 A100,
   同一支腳本要能兩用。預設仍是 A100-40GB,既有的 arm 重跑結果不變。

3. **C 的 exp1 那組只跑 sakai 兩段,沒跟著 E 補成 6 段。** 這是刻意的:
   E4B 要與 12B 比,就必須用**同一個子集**。E 讓 `Xgma_ja` 變成 6 段以後,
   拿 6 段的 0.272 去比 E4B 的 2 段 0.400 會是錯的比較。
   報告 §3F.2g 的表明確標了「sakai 兩段」。

4. **E 的結果寫進既有的 `Xgma_ja` arm 目錄,不另開 arm。** 模型、prompt、
   GPU、解碼參數全部相同,只有切塊路徑不同,而那一項逐檔記在各自 meta 的
   `n_chunks`(1 = 整檔單發)。任務書寫的就是「擴充 `Xgma_ja` 從 2 段到 6 段」。

5. **順手修了一個跨小節的聚合不一致(不在任務書範圍內)。**
   §3E.2 用實例 micro-average,§3F.2b 用段落平均,同一個 arm 兩個數字
   (Gemma 12B:0.056 vs 0.028;Gemini 3.5:0.927 vs 0.902)。
   **全部統一成 micro**,並在 §3F.2b 留了一則說明,免得舊數字對不上時被當成錯。

6. **切塊 handicap 的分析方法比任務書寫的多一步。** §4 E 寫的是直接比
   「切塊四段 vs 不切塊兩段」,但 sakai 兩段對**每個** arm 都比較簡單
   (`Cplus` 0.969 → 0.748),直接相減會把段落難度算進切塊頭上。
   改成先除以 `Cplus` 當難度基準再比。**這放寬了原規則的證據強度要求,
   所以結論仍照原規則的門檻判**(差 < 0.10 → 兩者相當),校正只是佐證。

7. **`modal run` 一次開太多 app 會撞 rate limit。** 同時送四個 `modal run`,
   其中一個回 `App create rate limit exceeded` 直接失敗(沒有花到錢)。
   重跑即可,但下次要分批送。
