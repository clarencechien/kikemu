# Handoff v7 — Oracle 天花板與錯誤互補性

**目的:在花任何錢之前,先判斷 GER / 多引擎融合這條路值不值得走。**

結論先講:**exp1 那一格完全不用花錢,現有檔案就能算完。** exp5 大概也可以。真正需要新跑的只有一小塊,而且不花 API 費用。

---

## 1. 要回答的問題

前面推導出的三段式架構(Breeze 出 N-best + SM 出時間戳與信心 + Gemini 做 GER)成本是兩倍、失效面積是三倍。動工前必須先確認它有沒有空間可拿。

**核心問題:正確答案在不在候選集裡?**

- 若各引擎錯在**同一批詞**上 → 融合沒有空間,GER 只能靠語意猜,**這條路直接放棄**
- 若錯開 → 可救回的量就是 GER 的天花板,再據此決定投不投

**附帶回答的第二個問題:錯誤是聲學造成的,還是模型造成的?**
若所有引擎在同一條件下一起崩,那是拾音問題,該投資麥克風(exp4 已有模擬),不是投資模型。

---

## 2. 免費的部分:現有檔案就能算

### 資料現況(已確認)

`results/noun_outcomes.json`(419 KB,3953 筆)結構:

```json
{"arm": "A", "cond": "N0", "seg": "hig01_A1", "noun": "絵馬殿", "strict": 0, "tolerant": 1}
```

**這正好是 oracle 需要的形狀。** 每個 (arm, cond, seg, noun) 的命中與否都在裡面,不需要重跑任何 arm、不需要碰音檔、不需要呼叫任何 API。

### 計算方式

對每個 `(cond, seg, noun)`,在 arm 集合 S 上取 OR:

```
oracle_hit(S) = any(hit[arm] for arm in S)
oracle_recall(S) = mean(oracle_hit) over all (cond,seg,noun)
```

**要算的三組 S:**

| 組合 | 回答 |
|---|---|
| 單一 arm(每個都算) | 基準線,應與既有報告的數字對得上 → **當作正確性檢查** |
| **全部 arm** | 絕對天花板 |
| **兩兩組合** | 哪一對最互補、值得融合 |

**每個聲學條件分開算。** 融合價值很可能隨噪音變化,而那正是選型時要知道的。

strict 與 tolerant **兩套判定都要算並分開報**,不要混用。

### 關鍵輸出:可救回集合

```
救得回 = oracle_hit 為真 且 1-best arm 未命中
```

把這些詞**逐筆列出來**,不要只給一個比率。因為要人眼看一下:救回來的是不是真的重要的詞(專名、術語),還是一堆無關緊要的。**比率會騙人,清單不會。**

### 錯誤互補性

對每一對 arm,算 Jaccard:

```
J = |兩者都錯的詞| / |至少一者錯的詞|
```

- J 接近 1 → 錯在同一批詞 → **融合無效**
- J 低 → 錯開 → 融合有空間

同時列出**所有 arm 都錯**的詞——那批詞是任何後處理都救不回的,它們定義了真正的天花板。若這批詞集中在某個條件,那是聲學問題不是模型問題。

---

## 3. exp5 的部分:大概也免費,但有個陷阱

先確認 `exp5/results/` 下有沒有同結構的 per-term outcome 檔。有的話同樣算法照跑,而且 exp5 多了 Breeze,是唯一能測「地端模型與雲端引擎是否互補」的資料。

**陷阱:exp5 的參考轉寫由 gemini-3.5-flash 產生。**

把 Gemini arm 放進 oracle 集合會虛增天花板——它跟參考同源。所以 **exp5 的 oracle 必須算兩版**:

- 含 Gemini arm(有主場偏差,標註)
- **不含 Gemini arm**(Breeze + SM,這是唯一乾淨的那一格)

報告以第二版為準,第一版只作參考。

**exp1 沒有這個問題**(參考來自大阪觀光局頁面),所以 exp1 的 oracle 是最可信的數字,應作為主要結論來源。

---

## 4. 現有資料回答不了的部分

以下需要新跑,但**都不花 API 錢**:

| 問題 | 需要什麼 | 成本 |
|---|---|---|
| **同引擎 N-best 的 oracle** | Breeze beam search(`num_beams=5, num_return_sequences=5`) | 本機 GPU 時數 |
| **候選多樣性** | 上述候選兩兩間的平均編輯距離 | 幾乎零 |
| 字元級 oracle(不只術語) | 對齊多份逐字稿 | 純計算 |

**但這些先不要做。** 順序是:

1. 先算跨引擎 oracle(免費)
2. **若跨引擎 oracle 沒有明顯空間,同引擎 N-best 更沒有** —— 不同引擎的分歧一定大於同一個引擎 beam 內的分歧
3. 只有跨引擎有空間時,才值得花 GPU 時間去看 beam N-best 能不能再加一點

> 已知風險:Whisper 家族的 beam N-best 多樣性通常很低,候選之間常只差標點或虛詞。所以第 3 步要**先量多樣性再量 oracle**,多樣性太低就直接放棄,不用跑完。

---

## 5. 判讀與決策門檻

跑完之後,依 exp1 的乾淨數字判斷:

| Oracle 減最佳單一 arm | 結論 |
|---|---|
| < 0.03 | **放棄融合與 GER。** 候選集裡沒有答案,加再多層只是增加幻覺風險與除錯難度 |
| 0.03 ~ 0.10 | 邊際。只在「專名錯一個就毀掉整份」的場景(料號、法遵)才考慮,且必須有人校對 |
| > 0.10 | 值得做。進入 B0~B3 的消融實驗設計 |

**同時檢查:救回清單裡的詞是不是重要的。** 若 oracle 高但救回的全是無關緊要的詞,實際價值仍然是零。

---

## 6. 產出

```
/analysis
  oracle.py              # 純計算,不呼叫任何 API
  complementarity.py
/results
  oracle.json            # 各 arm 組合 × 條件 × strict/tolerant
  oracle_recoverable.md  # 可救回詞的逐筆清單(人眼檢視用)
  oracle_unrecoverable.md # 全 arm 皆錯的詞
  oracle_report.md
```

**正確性檢查(必做):** 單一 arm 的 oracle 必須等於既有報告中該 arm 的召回率。對不上就是資料或判定規則有問題,先修再往下算。

---

## 7. 報告要回答的

1. exp1 各條件下:最佳單一 arm vs 全 arm oracle 的差值
2. 哪一對 arm 最互補(Jaccard 最低)
3. 可救回詞的清單與性質判讀(是不是重要的詞)
4. 全 arm 皆錯的詞集合 —— 是否集中於特定聲學條件
5. exp5 的乾淨版(Breeze + SM,不含 Gemini)oracle
6. **一句話建議:GER / 融合這條路,做或不做**

---

## 8. 已知侷限(要寫進報告)

- 跨引擎 oracle 是「事後最佳選擇」的上限,實務上 GER 拿不到全部
- 但它也**低估**了 GER 的能力:GER 能跨候選重組出沒有任何單一 arm 產生過的正確答案(這正是 GER 勝過傳統 rescoring 的原因)。所以這個數字是一個**參考帶**,不是精確上下界
- 術語級 oracle 不等於字元級 oracle,兩者可能給出不同結論
- exp1 為日文導覽語料;結論外推到中英夾雜(exp2)或領域外(exp5)需另行驗證

---

## 9. 為什麼要先做這個

**這一步的成本是幾小時的純計算,而它可能省掉一整條管線的開發。**

跟 kikemu 先量「詞表邊際價值」再決定押不押它,是同一個判斷順序:**先量天花板,再決定要不要爬。**

---

## 10. 執行結果(2026-08-19,由 `results/oracle_report.md` 回填)

**結論:不做 GER / 融合;改做非即時的「並排雙跑 + 人工複核」。**

- §2 的免費計算全部成立,`results/noun_outcomes.json` 正是需要的形狀。
- §6 的正確性檢查通過:單一 arm 的 oracle 全部等於既有報告的召回率。
- §5 的門檻判讀:exp1 全 arm 增益 +0.045 / +0.045 / +0.090 / +0.134 / +0.075。
- **本任務書沒要求、但決定結論的一步**:把 arm 拆成「同一產品情境下能同時跑」
  的子集。即時線在最需要幫助的兩格只有 **+0.015**,而 N3 的 +0.134
  全部來自現場拿不到的批次 arm。
- §3 的 exp5 乾淨版(Breeze + SM,排除與參考同源的 Gemini):+0.013 / +0.019,
  兩格都在放棄門檻以下。
- §4 的同引擎 N-best **不用做**:對照組已量到同一家引擎兩種設定 J = 0.89~1.00、
  五個條件裡四個增益 0.000。beam 內分歧只會更小。
- 第二輪補跑 `Abat` 的 N1/N2(12 檔,不花 API 錢),補齊唯一有缺格的證據;
  N2 因此從 +0.015 變成 +0.090,跨了一個判讀等級——**缺格不能當保守估計交差。**
