# Handoff v11 — 非即時雙跑配對:量互補性

**目的:用數據決定非即時線的引擎配對,取代基於架構推論的暫定選擇。**

零 API 成本。用既有的 per-item outcome 檔計算。

> **命名偏離:任務書原標題是「Handoff v8」,但 `handoff-v8.md` 已經被
> Modal 補測任務書佔用(2026-08-19)。改編為 v11(v9 = E4B/12B 補洞、
> v10 = Scribe),內容一字未改。**

執行結果:[`results/pairing_report.md`](results/pairing_report.md)、
[`results/pairing.json`](results/pairing.json)、
`analysis/complementarity_pairs.py`。

---

## 1. 要推翻的推論

目前 `offline-meeting-architecture.md` §3 建議 **SM + Gemini**,理由是:

> 互補性來自引擎家族 → Scribe 與 Gemini 都偏生成式、SM 是逐幀同步 →
> SM 家族最遠、最互補

**這條推理鏈有兩個弱點:**

1. **Scribe 的解碼器架構未公開。** ElevenLabs 沒揭露聲學模型;「偏生成式」
   是從 `logprob` 欄位命名、字元級時間戳、句內自動多語推測的,不是已知事實。
   **拿一個猜測的家族標籤當選型依據,不夠。**
2. **exp1 證明的是「換設定沒用」(同引擎不同設定 J = 0.89~1.00),
   不等於「換家族就有用」。** 後者是合理外推,不是量到的。

而反方向的證據不弱:Scribe 批次勝 SM +0.096,N3 人群 8dB 拿全場最高 0.657,
且同樣具備 diarization(up to 32)與詞級 + 字元級時間戳。

**所以配對應視為未定案,由本實驗決定。**

---

## 2. 計算

沿用 oracle 那套腳本(`analysis/complementarity.py`),
資料來源 `results/noun_outcomes.json` 與 exp5 對應檔案。

### 2.1 兩兩 Jaccard

對每一對 arm,在 `(cond, seg, noun)` 上算:

```
J = |兩者都錯的詞| / |至少一者錯的詞|
```

- J 接近 1 → 錯在同一批詞 → 融合/雙跑無效
- J 低 → 錯開 → 有互補空間

**要算的三對(批次版本):**

| 對 | 目前假設 |
|---|---|
| SM 批次 × Gemini 批次 | 暫定推薦 |
| **Scribe 批次 × Gemini 批次** | 分數與功能較優的挑戰者 |
| SM 批次 × Scribe 批次 | 兩個專用引擎的距離 |

**每個聲學條件分開算**,strict / tolerant 兩套判定分開報。

### 2.2 配對 oracle

每一對的 oracle 召回率(逐項 OR),減去該對中較佳單一 arm。

**這是「這對雙跑最多能拿到多少」的上限。**

### 2.3 勝率分佈

對每一對,在分歧處統計各自命中的比例
(參照 §8b 已做過的 Gemini 78% / SM 22%)。

**勝率愈接近五五開,複核愈有價值**(真的在做選擇,不是蓋章);
勝率愈懸殊,愈接近「用強的那個單跑就好」。

---

## 3. 前置:補齊缺格

**exp5 的比較表未含 SM 的數字。**

若 `exp5/results/` 下沒有 SM 的 per-item outcome,**必須先補跑**,
不得以「保守估計」帶過。

> 依據:exp1 初版 `Abat` 缺 N1/N2 曾被記為「保守估計」,
> 補跑後 N2 從 +0.015 變 +0.090,跨一個判讀等級。

同樣檢查 Scribe 的條件覆蓋是否完整(N0~N4 / M0~M3)。
**缺哪一格就補哪一格,補不了就在報告中標明該格結論不成立。**

---

## 4. 正確性檢查(必做)

單一 arm 的 oracle 必須等於既有報告中該 arm 的召回率。
對不上就是資料或判定規則有問題,先修再往下算。

---

## 5. 判讀規則(先寫死,避免事後找理由)

依 **exp5(領域外、乾淨語料)** 為主,exp1 為輔。

**決策順序:**

1. **取 J 最低的一對** 作為互補性最佳
2. 若最佳與次佳的 J 差距 < 0.05(在 bootstrap CI 內重疊)→ **視為打平**,
   改用第二判準
3. 第二判準:**該對的配對 oracle 增益**
4. 仍打平 → 第三判準:**功能完整性**(diarization、詞級時間戳、地端可得性)

**功能備註(不參與前兩輪判讀,但影響最終管線):**

| | diarization | 詞級時間戳 | 字元級 | 地端 |
|---|---|---|---|---|
| Speechmatics | ✅ | ✅ | — | ✅ 容器 |
| Scribe v2 | ✅ up to 32 | ✅ | ✅ | ❌ |
| Gemini | ❌ | ❌ | ❌ | ❌ |

**Gemini 不提供結構**,所以另一支必須扛 diarization 與時間戳。
SM 與 Scribe 都能扛 → **這一項不構成 SM 的獨佔優勢**
(這點修正了原架構文件的論述)。

---

## 6. 三跑值不值得(附帶回答)

若三個 arm 兩兩的 J 都低,代表三者各自互補。

計算 **三跑 oracle 減最佳雙跑 oracle**:

- 增益 < 0.03 → 不值得。第三支只增加複核負擔
- 增益 ≥ 0.03 → 記錄下來,但**仍須以複核負擔(分歧率)評估**,不能只看 oracle

> 提醒:oracle 沒有計入「選錯」的成本。三跑的分歧率會顯著高於雙跑,
> 而複核者的時間是真實成本。

---

## 7. 產出

```
/analysis
  complementarity_pairs.py     # 純計算,不呼叫 API
/results
  pairing.json                 # 各對 × 條件 × strict/tolerant
  pairing_report.md
```

## 8. 報告要回答的

1. 三對的 Jaccard 矩陣(exp5 為主,exp1 為輔;各條件分開)
2. 各對的配對 oracle 增益
3. 各對在分歧處的勝率分佈
4. 依 §5 的判讀順序得出的**配對結論**
5. 三跑的增益與複核負擔評估
6. 缺格清單與其對結論的影響
7. 一句話:非即時線的配對定案

## 9. 完成後要更新的文件

- `offline-meeting-architecture.md` §3 —— **把「建議 SM + Gemini」改成量測結論**;
  若結論是 Scribe + Gemini,§7 的地端分支要另外處理(Scribe 無地端)
- `results/stt-matrix.md` —— 補一條規則:
  **架構家族推論不足以決定配對,必須量 Jaccard**
- 若 Scribe 勝出,`§5 功能表`要成為新的管線基礎

---

## 10. 為什麼要做這個

原建議是**用一個推測的架構標籤,推導出一個可以直接量的東西**。

量它的成本是幾小時純計算;猜錯的成本是整條非即時線用了較差的配對,
而且不會有人發現——因為兩者都會產出看起來合理的逐字稿。

---
---

# 11. 執行紀錄(2026-08-20 完成)

## 11.1 驗收

- [x] §3 缺格檢查:**exp5 三個引擎 M0/M3 全滿(83 項 × 2),不需補跑**
      ——任務書 §3 的前提「exp5 未含 SM」是錯的(見 §11.3 偏離 1)
- [x] §4 正確性檢查:exp5 3 arm × 2 條件 × 2 判定 + exp1 3 arm × 5 條件 × 2 判定,
      **全部與 `scores.json` 獨立重算的召回率相符**(容差 5e-4)
- [x] §2.1 Jaccard:三對 × 各條件 × strict/tolerant,**外加 pooled 與 bootstrap CI**
- [x] §2.2 配對 oracle 增益:逐條件 + pooled,**並另報絕對天花板**(見偏離 3)
- [x] §2.3 勝率分佈:逐條件 + pooled
- [x] §6 三跑:pooled 四格全部 < 0.03 門檻
- [x] §7 產出:`analysis/complementarity_pairs.py`、`results/pairing.json`、
      `results/pairing_report.md`
- [x] §9 文件更新:架構文件 §3/§7、`stt-matrix.md` 規則 9

## 11.2 §5 的落點

| 步驟 | 規則 | 落點 |
|---|---|---|
| 1 | J 最低的一對 | exp5 pooled:SM×Gemini 0.178 |
| 2 | 差距 < 0.05 或 CI 重疊 → 打平 | **打平**(差 0.039、CI [−0.188,+0.161] 含 0) |
| 3 | 第二判準:配對 oracle 增益 | **Scribe × Gemini**(+0.048 對 +0.038,strict 亦同) |
| 4 | 第三判準:功能完整性 | **未使用——判讀在第 3 步結束** |

**配對定案:Gemini 批次 + Scribe 批次。**

**結論不依賴第三判準**,所以也不依賴 §5 那張功能表、
更不依賴 §1 質疑的那個架構家族猜測。

## 11.3 偏離紀錄

1. **§3 的前提是錯的,所以「補跑」那一步沒有執行。**
   exp5 的 `Xbat_bi`(SM 批次)per-item outcome 是滿的(83 × M0/M3)。
   誤解來源:`offline-meeting-architecture.md` 原文寫「exp5 未含 SM」,
   任務書照抄。**那份文件收進 repo 時已對帳改掉,任務書是在改之前寫的。**
   → 真正的缺格是 **exp5 沒有 M1/M2**(沒有噪音曲線,既有侷限 22),已在報告標明。

2. **§5 沒說跨條件怎麼合併,而逐條件判會自相矛盾。**
   exp5 的 M0 第二判準指向 Scribe、M3 指向 SM。
   **補了 pooled(所有條件合併)並以它為主**,理由是 §5 的「依 exp5 為主」
   講的是語料層級。**這是補的判斷,不在任務書裡。**

3. **§5 的第二判準有瑕疵:「配對 oracle 增益」會獎勵低下限。**
   M3 就是實例——SM×Gemini 增益 +0.076 高於 Scribe×Gemini 的 +0.044,
   但那只是因為它的最佳單一 arm 只有 0.817(Scribe 那對已站在 0.899);
   **絕對天花板是 0.892 對 0.943**。這與鐵律 8 是同一件事。
   **照規則判(仍得 Scribe),但另外報了絕對天花板當對照**,兩種讀法結論相同。

4. **⚠️ 主判讀語料有參考文本污染,任務書沒有考慮到。**
   exp5 的參考是 `gemini-3.5-flash` 產的,而主判讀的 Gemini arm 就是它。
   `Gbat` 在 M0 召回 0.987 → 含 Gemini 的兩對 J 都被系統性壓低。
   §5 說 exp5 是「乾淨語料」,但那個乾淨是**聲學**乾淨,不是**參考**乾淨
   (`oracle.py` 早就標了 exp5 `clean_ref: False`、exp1 `True`)。
   **仍照規則以 exp5 為主,但把 exp1 完整並列**;
   幸好兩者的主要判準一致,污染沒有改變方向。**若不一致應以 exp1 為準。**

5. **bootstrap CI 在這個資料量下沒有鑑別力。** 重抽單位是 segment,
   exp5 只有 3 個 cluster、exp1 6 個,CI 寬到幾乎必然含 0。
   **報了,但明確標明「CI 含 0」只代表「無法區分」,不是「證明打平」。**

6. **命名:任務書標題是 v8,但 `handoff-v8.md` 已被佔用**,改編為 v11。

## 11.4 這次最值得記住的一件事

**規則跑完之後,最有說服力的數字不是規則權重最高的那個。**

§5 把「J」放第一、「增益」放第二,而兩者都只給出**微弱**的方向
(差 0.039、差 0.010,都在雜訊裡)。真正壓倒性的是排在 §2.3、
**完全沒有進入決策順序**的勝率:

> exp5 的 SM × Gemini 分歧 **88 處**,SM 只在 **14%** 的分歧處是對的。
> 複核者看 88 個地方,86% 的答案都是 Gemini。**那是蓋章,不是選擇。**
> Scribe × Gemini:分歧 **36 處**、勝率 **0.58**。
> **複核量少 59%,而且每一次都真的在選。**

而勝率量的正是這條產品線真正的成本——**複核者的時間**。
**下次寫判讀規則,要問「哪個量最接近真正的成本」,再決定它排第幾。**
