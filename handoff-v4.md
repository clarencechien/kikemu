# Handoff v4 — 指向性拾音:Speak2 級夠不夠,還是要到領夾麥?

接在 exp3 之後。exp3 證明軟體降噪救不了任何一邊;本實驗測**拾音物理**——
同一套 harness、同一批語料與正解,把「換麥克風」翻譯成聲學參數重新合成。

---

## 1. 要回答的問題

1. **Jabra Speak2 這一級的會議陣列麥(桌面、波束成形)有沒有幫助?幫到誰?**
   - 對一體式(Gemini Live):能不能把 N2/N3 的崩潰救回可用區(≥0.7)?
   - 對 SM+詞表:本來就 0.6~0.8,還有多少上升空間?
2. **要不要到領夾麥(0.3m 近講)才算解決?** 領夾條件下 Gemini Live
   是否回到自己的 N0 水準(0.836)——若是,吵雜場景可以改走便宜的一跳架構
3. **manemu(對話口譯線)用 Speak2 會更好,還是其實不需要?**
   - manemu 的假設場景是近場手機(人對著手機講),那本來就 ≈ M0/N0
   - 真正的問題是「會議室裡開著 manemu」:exp2 實測 12dB 會議交談
     會讓一體式掉到 0.396——Speak2 級拾音能不能把這個場景救回來?

## 2. 方法:指向性的聲學模擬(不是買硬體)

### 2.1 原理

直達音服從平方反比,殘響場與擴散噪音在房間內近似均勻。
指向性與距離的效果可以歸結為兩個參數:

- **ΔDRR**(direct-to-reverberant ratio 改善):距離減半 +6dB;
  心形 DI ≈ 4.8dB;槍型 8~10dB;4~8 麥波束陣列 9~13dB;領夾 0.3m 對 3m 遠場 ≈ +18dB
- **ΔSNR**(對擴散人聲噪音的改善):同源,但保守估計要打折——
  人聲噪音源可能落在波束內(桌邊其他人),陣列的 null 不保證對準它們

### 2.2 合成

沿用各實驗的 RIR 與噪音(同種子、同切片,配對比較乾淨):

```
y = x ⊛ h_de + β·(x ⊛ h_late) + γ·noise
h_de   = RIR 直達+早期(首達 ±50ms)
h_late = RIR 晚期殘響尾
β = 10^(−ΔDRR/20),γ 依 ΔSNR 重算(語音活動段 RMS,同 degrade.py)
```

### 2.3 硬體 profile(寫死,對應可購買的東西)

| profile | 模擬對象 | ΔDRR | ΔSNR | 依據 |
|---|---|---|---|---|
| base | 手機遠場直入(= 既有 N*/M*) | +0 | +0 | — |
| **card** | 心形手持/桌面單指向麥,1m | +5 | +3 | DI 4.8dB,噪音折半計 |
| **conf** | **Speak2 級 4 麥波束陣列,1.5m 桌面** | +9 | +6 | 陣列 DI 9~13dB 取下緣;噪音保守折計 |
| **lav** | **領夾麥 0.3m** | +18 | +15 | 3m→0.3m 平方反比 +20dB 取保守值 |

**模擬的侷限先寫死:** 真實 Speak2 還有自己的 DSP/AGC(exp3 說明這可能是負資產)、
波束對準假設理想、人聲源方位未建模。模擬結論若是「conf 夠」,**必須**接真機驗證
才能下採購結論;若是「conf 不夠、lav 才夠」,反而可信(模擬取的是樂觀上緣)。

## 3. 實驗矩陣(兩階段控成本)

**階段一(SM batch,便宜快速):**
全 profile × {N1, N2, N3} × 6 段 → SM batch + 詞表。
回答「SM 還有多少上升空間」+ 合成 sanity check。

**階段二(即時,貴):**
| 誰 | 條件 | 回答 |
|---|---|---|
| Gemini Live(exp1 語料) | {conf, lav} × {N2, N3} × 6 段 | Q1/Q2 主體 |
| Gemini Live(exp2 語料) | conf × {M2, M3} × 3 段 | Q3(manemu 會議場景) |
| SM 即時 + 詞表 | 補跑 conf × N3(若 batch 顯示有感提升) | Q1 的 SM 側 |

即時推流合計約 60~75 分鐘 wall-clock。

## 4. 指標與判讀(先寫死)

主指標:專名/術語召回率(tolerant,沿用各實驗凍結規則)。

| 觀察 | 結論 |
|---|---|
| A@N3+conf ≥ 0.7 | **Speak2 級就能救回一體式**;吵雜場景可改一跳架構,採購結論=會議麥,接真機驗證 |
| A@N3+conf < 0.5 且 A@N3+lav ≥ 0.75 | **要到領夾才解決**;導覽線的正解=導覽員戴麥,散客線維持 SM+詞表 |
| A@N3+lav < 0.7 | 連近講都救不完全,一體式在人聲環境徹底出局(不太可能,若發生要查合成) |
| C+@N3+conf − C+@N3 ≥ +0.1 | SM 也吃得到指向性紅利,兩條線都該配好麥 |
| C+@N3+conf − C+@N3 < +0.05 | SM 已在天花板附近,好麥的錢應優先花在一體式能否降級 |
| G@M3+conf ≥ 0.75(≈其 M1/M2 水準) | **manemu 配 Speak2 值得**:會議室場景被救回 |
| G@M3+conf < 0.6 | manemu 在多人交談的會議室仍不可用,Speak2 不必買;近場對話本來就不需要它 |

## 5. 產出

```
scripts/directivity.py            RIR 拆解 + profile 合成(exp1/exp2 通用)
corpus/conditions_dir/            合成檔(不入庫)
exp2/corpus/conditions_dir/
results/raw/<arm>_dir_<profile>/  各 run 完整回應
results/report.md                 新增 §「指向性實驗(exp4)」
results/stt-matrix.md             更新情境矩陣的拾音建議欄
```

## 6. 成本

SM batch 額度內;Gemini Live 約 90 分鐘音訊 < $1;總計 < $2。

## 7. 開工順序

1. directivity.py:RIR 首達偵測 + 直達/晚期拆解,先對一個檔做能量檢核
   (拆解後 β=1, γ=1 重合成應與原 N* 檔幾乎相同,CER 級驗證)
2. 產全部合成檔
3. 階段一 SM batch 篩選
4. 階段二 Gemini Live(exp1 conf/lav、exp2 conf)
5. 依判讀表出結論,更新報告與 stt-matrix,標注「模擬待真機驗證」
